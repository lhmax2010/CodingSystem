# 01_Contract_Spec v0.2 三方复审处置表（→ v0.3）

裁决输入：Kimi NO-GO（1 MAJOR + 3 MINOR/NIT + 1 CLARIFY，其余全接受）、Claude Code NO-GO（3 MAJOR + 4 MINOR + 3 NIT + 1 CLARIFY，首轮 24 条全关闭）、Codex NO-GO（13 条不接受重开 + 5 BLOCKER + 4 MAJOR + 1 NIT）。合计 47 条，**全部采纳**。
**V-13b 勘误：三方规则+程序双表态全 GO，正式生效**——已回写 HLD 冻结版附录 A15（三家一致建议的单一事实源处理）。

## 共识簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| DA §6 注册表不完备（三家 B-4 同反对） | Kimi M-01/M-02; CC P-03; Codex CS-25 不接受 | §6 全量重写为逐 case 行（前置/动作/期望），补 9 个缺失族：CF-DSL-PROP（随机图 property）、CF-LED（逐提交点参数化 kill 注入通则）、CF-SM-08（恢复期 D12→C-14）、CF-SM-09（publishing 重启对账优先）、CF-SM-11（gate TTL）、CF-COR-01..03、CF-REC-01..02、CF-UP、CF-FIX-01..05（五类 fixture，G6 判定依据）；矩阵型 case 定轴+单元格期望规则，07 枚举执行——"权威的代价是完备"（CC 语）照单全收 |
| DB 旗舰 wire 不兼容（HLD 回归） | Codex R2-01 + CS-04/CS-06 不接受 | §1.1：from alias、标量 to 规范化、value 位置词素级预处理（十进制字面无损转定点、超 scale 拒绝）；CF-DSL-FLAG 旗舰原文直接加载断言 |
| DC 注册/attestation 规则丢失 | CC P-01（+C1 部分接受）; Codex R2-02 | V-02 占位启用承载（注册存在+status 非空+task_prefix 唯一+contract 兼容+attestation 绑定 manifest 制品 digest，先于 V-05）；CF-DSL-02 |
| DD gate TTL 无契约表示 | CC P-02 | C-08c gate_ttl_expire（编排合成 outcome=gate_expired、不经 ApprovalDecision、outbox origin=ttl）；CF-SM-11 |
| DE canonical 残留 | Codex CS-01 不接受; Kimi M-03/M-04; CC C-a | §0.1：递归禁浮点校验（覆盖 config/ext/params 嵌套）、frozenset→排序列表序列化、datetime 唯一字面、NFC 碰撞拒绝、转义规则、定点 pattern 强制（声明 scale）、整数 2^53 界 |
| DF 标识符生成方矛盾 | Codex CS-02 不接受 | §0.2：decision/adjudication/correction_id 改服务端生成（载荷不含）；ApprovalDecisionRequest/Decision 拆分；task_prefix 格式与唯一性入 V-02；snapshot 全量 digest 判定；task_id 位数注记（Kimi M-05 同解） |

## Codex（重开 13 + R2-01..R2-10）——全采纳
不接受重开逐条：CS-01/02/03（`==dev*` 非法 → 锁精确 devN）/04/05（V-09 改互斥性判定，其反例形态 CF-DSL-09c 固化合法）/06/10（PendingEvent 判别联合带消费载荷）/12（ReportBase 定型 + 具名载荷 + A0 对账；v0.2 自立"无匿名 schema"标准未达，重开成立）/16（C-09 目标列修正 + ci_plan_task_id 来源闭合 + broker digest 重建比对）/19（supports_probe 静态位 + probe_retry 参数 + 编排进程执行定案 + 重试计数持久化）/22（change_id 改 broker 生成回填，与 CC P-05 合并）/23（密级权威=Ledger 索引、blob_begin 出生标注、自报不符拒绝）/24（TargetSpec 解析语义 + 具名模型 + TraceEvent 定型 + IDEM 码统一）/25（簇 DA）。
新增：R2-01（簇 DB）；R2-02（簇 DC）；R2-03 → §3.3a 初始 series 单事务（SER-0/lineage/tree_digest/current/重放语义 + CF-SER-06）；R2-04 → ReportBase + HandoffResult 键约定 + ReleasePlan 继承头（与 CC P-06/P-07 合并）；R2-05 → EffectRecord.params_ref + effect_claim 封闭响应联合 + probe 进程定案；R2-06 → §3.5 字节编码/git 步骤/OID 表示/repo_id 字符集/golden vectors；R2-07 → create_run 异步定案（响应 {run_id,state}，snapshot 经 GET）；R2-08 → C-15a/b 拆分；R2-09 → FeedbackReport.reports 改 ReportEntry 列表；R2-10 → 57 条计数修正。

## Kimi / Claude Code 其余（全采纳）
Kimi：M-02（CF-LED 通则即其参数化建议）；M-03/M-04（簇 DE）；M-05（task_id 注记）；CL-1 → V-15 补 field 唯一匹配规则。
CC：P-04（probe_retry 字段）；P-05（并入 CS-22）；P-06/P-07（并入 R2-04）；T-b1（"理论不可达"措辞修正——其反例成立：fix-patch candidate 首次应用即在完成事务）；T-b2（EffectType 统一）；T-b3（value pattern 经加载器预处理挂点）；C-a（2^53 界）。B-4 反对成立（簇 DA）；其"loop activation 空壳有界累积无害"观察记录在案，不动作。

## 汇总
- 47 条全采纳，无裁剪；v0.3 修改集中于 wire 兼容、初始 series、审批链闭合、probe 参数持久化、§6 扩全五条主路径（恰为 Codex 一句话总结所列）。
- 仲裁记录一条：v0.2 处置表所称"45 条"为笔误，实为 57 条原始（约 40 独立），已在 A3 更正——处置计数的诚实性问题，致歉并留痕。
- v0.3 复审重点：(1) 簇 DB 修复后旗舰原文加载（CF-DSL-FLAG 可实证）；(2) §3.3a 初始 series 与 §3.3 的事务边界；(3) §6 扩全后作为 07 权威的充分性复核；(4) effect_claim 响应联合与对账协议的接合；(5) V-13b 已生效，本轮不再表态、只核 HLD A15 回写与 01 文本一致。
