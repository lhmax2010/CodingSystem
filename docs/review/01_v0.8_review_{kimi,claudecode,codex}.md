## 1. 总裁决：NO-GO

存在 4 项 BLOCKER：broker 恢复核销路径缺失、control-plane wire 仍未封闭、幂等豁免与 HLD 冲突、probe 丢失路径定义互相矛盾。

## 2. 任务 A 结果表

| 本方第七轮编号 | 结论 | 理由与定位 |
|---|---|---|
| R7-01 | 接受关闭 | §0.1 已明确 StrEnum→字符串、IntEnum→整数；与 §5.3 `DataClass(IntEnum)` 及既有 golden vector 一致。 |
| CS-24 / R6-02 / R7-02 | 不接受 | §5.1 已补 command 字段和授权响应，但仍存在 D12 两种 wire 表示、无统一成功/错误响应联合、注册表使用省略号、`CommandAck/CommandError` 未模型化等问题；见 R8-02。 |
| R6-01 | 不接受 | §5.1 新增 heartbeat/trace receipt 豁免后，无法继续兑现同 request_id 异载荷拒绝；编排→broker 出向 receipt 也没有符合 Ledger 单写者约束的持久化路径；见 R8-03。 |
| R5-02 | 接受关闭 | §5.1 已统一 `request_id` 位于 CommandEnvelope，AdjudicateRequest 不再重复携带；`adjudication_id` 由编排事务签发。 |
| R4-04 / R6-06 / R7-03 | 不接受 | “worker 已启动后退出”原反例已有合成 receipt，但新文本又产生 replay-safe 终态矛盾、`origin` 无承载模型、真实 spawn 失败未归类及 CF-EF-10 未同步等问题；见 R8-04。 |
| R4-03 / R6-05 / R7-04 | 不接受 | 指定的五类 negative vector 已可判断，但 DNS label、端口范围、path 字符域及 `inprocess` 绑定仍未封闭；见 R8-05。 |
| R7-05 | 接受关闭 | CF-FIX-00 已将 Agent route/status 与 gate outcome 分开校验，两份 YAML 可同时通过。 |
| R7-06 | 接受关闭 | 原缺陷“candidate 只存 digest、分支 B 依赖进程内 patch”已由 `payload_ref` 消除；新增的重启/attempt 接合及 artifact 域问题另列 R8-06、R8-07。 |

## 3. 任务 B 结果

1. **反对——命令注册表与响应联合尚未封闭。**  
   `authorization_consume` 在 broker 恢复时没有合法状态机入口；D12 同时可表示为 `ConsumeD12Mismatch` 和 `CommandError(E-CP-D12-MISMATCH)`；通用成功/错误响应也没有判别 envelope。broker/worker 客户端仍可能形成不同实现。

2. **反对——两条给定时间线的核心计数思路成立，但外围语义不闭合。**

   - worker 每次成功启动后在回报前退出：每组可合成 indeterminate 并消耗一次，次数有界；耗尽后应依 §4.3 矩阵区分 replay-safe 与非 replay-safe，而不是无条件到 `unknown+sys.error`。
   - 编排每次在 spawn 前崩溃：恢复重派同组且不递增，不产生假耗尽；服务稳定后可继续。
   - 但 spawn 调用明确失败、合成 receipt 的 `origin` 字段以及权威 CF-EF-10 期望均未定全，故整体不能接受。

3. **反对——列举的 negative vectors 可判定，但完整接受域仍不唯一。**  
   尾点域名、非规范 IPv6、前导零端口、显式 `:443`、点段路径均可按当前文字拒绝；`inprocess:` 字面也会进入 provider digest，能参与 D12 复核。但 DNS label、端口范围、path 字符集及 `inprocess:<provider_id>` 是否必须匹配外层 provider_id 尚未定义。

4. **反对——payload 已持久化，但“编排重启后走原 candidate 的分支 B”时间线不闭合。**  
   按 HLD §5.1.2，编排失联会使 worker 因 socket EOF 退出；若恢复以新 attempt 重执行，§3.3 会先 discard 旧 candidate，CF-SER-11 无法消费它；若保持原 attempt，则新 worker 如何继承 request_id/candidate_id 未定义。

5. **接受——CF-DSL-FLAG 与 CF-FIX-00 可同真。**  
   两份原文中 `on` 均不匹配 bool/int/null 表而保持字符串；`5.0` 先保持原词素再规范化为 `"5.00"`；A4 的 `true` 唯一解析为布尔；`join:pre_final_review` 保持完整字符串。旗舰全部 Agent outcome 均进入对应注册集，gate 的 pass/approved/rejected 独立对照 §1.2；A4 的 review terminal `pass` 也已注册。

## 4. 新 issue 清单

| 编号 | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|---|---|---|---|
| R8-01 | BLOCKER | §2.1 C-09；§2.2(a)；§5.1 AuthorizationConsumeResponse；CF-AUTH-05；HLD §8.3 | C-09 只允许 `running` 且把“有效”限定为未核销；但 CF-AUTH-05 与 HLD §8.3 明确要求 broker 恢复时，`publishing` 且 `consumed_by_task_id` 为本 task 的授权仍视为有效。当前唯一命令 `authorization_consume` 此时会被状态 guard 拒绝或落入 `already_consumed`，没有“继续发布”的可执行出口。 | 明确定义恢复分支：`publishing ∧ consumed_by_task_id==task_id ∧ 全绑定仍匹配` 返回可继续发布的 granted/resumed 结果，不重复核销、不迁移状态；被其他 task 核销须单独 fail-closed。同步 C-09、响应联合及 CF-AUTH-05。 |
| R8-02 | BLOCKER | §0.4；§5.1 命令载荷块/注册表；§2.2(a)；CF-IPC-06 | 所谓三列封闭注册表仍使用 `blob_commit/abort→…` 等省略表达；`CommandEnvelope.command` 实际为任意 `str`；`CommandAck`、`CommandError` 只有行内文字而无具名模型；未定义每个成功响应与 CommandError 的 wire 判别方式。更直接的冲突是 D12 同时定义为 `ConsumeD12Mismatch{result:"d12_mismatch"}` 和 `CommandError{code:"E-CP-D12-MISMATCH"}`。 | 给出无省略的逐命令注册表；将 command 定为封闭 Literal/StrEnum；模型化 Ack/Error，并定义有 discriminator 的统一响应 envelope；D12 只保留一种 wire 表示。CF-IPC-06 按最终全部方向和响应分支逐项展开。 |
| R8-03 | BLOCKER | §5.1 通用 RPC envelope；HLD D6、§7.5、§8.5 | HLD §8.5 无例外规定“命令幂等（request_id）”，但 §5.1 让 heartbeat/trace_append 免 receipt。仅保存最新 heartbeat seq 无法对“同 request_id、不同 seq”返回 mismatch；trace 仅以首 seq 去重也无法区分同首 seq、不同后续事件的批次。此外，`release_dispatch` 的 receiver 是无 Ledger 写权限的 broker，但规范又要求其受理即持久化首响应 receipt，未说明合法写入路径。 | 保留所有命令的 scoped request_id+payload digest 语义，可对高频命令采用压缩/滚动保存但不能取消异载荷检测；定义 broker 通过何种编排 RPC/事务登记 durable inbox 与首响应。若坚持语义豁免，须先走 HLD 勘误流程。 |
| R8-04 | BLOCKER | §4.3 Probe；§4.2 迁移表；CF-EF-02、CF-EF-10 | 合成 receipt 要求 `origin=worker_loss`，但 ProbeResult/ProbeReport 均无该字段，也没有 ProbeReceipt 模型；同段声称连续丢失必到 `unknown+sys.error`，与 §4.3/CF-EF-02 的“replay_safe 时 abandoned+重放”冲突。另有“未 spawn 不消耗”与“每次 spawn 失败消耗”的文字矛盾：spawn 明确返回失败既没有启动记录，也不满足“worker 启动后退出”，可能永久重派。§6 权威 CF-EF-10 行也未写入新增连续丢失分支。 | 定义具名 ProbeReceipt 及 origin；把未尝试、spawn 明确失败、已启动后丢失三种状态分别定案；耗尽后严格复用 §4.3 能力矩阵。将合成回执、连续丢失和 replay_safe/非 replay_safe 两种终态写入 CF-EF-10。 |
| R8-05 | MAJOR | §5.3 ProviderExecutionConfig；CF-SM-12 | “完整词法文法”仍依赖未内联的“dns 名”等概念。例如 DNS label 首尾连字符、空 label、长度边界，端口 0/超范围值、path 中空格/控制字符均无明确结论；`inprocess:<provider_id>` 是否必须等于记录的 provider_id 也未规定。不同实现仍可能接受不同字符串，导致 D12 digest 行为分裂。 | 给出自-contained lexical grammar或唯一规范性语法引用，并补 label/总长、端口范围、path 字符域和 inprocess 绑定约束；为每个边界给出具体正反向 vector 与 case ID。 |
| R8-06 | MAJOR | §3.3 CandidateRecord；CF-SER-10/11；HLD §5.1.2、§4.3.3 | CF-SER-11 要求编排重启后继续以旧 candidate 进入分支 B，但真实进程重启会使 per-task socket 断开并终止旧 worker。规范没有决定恢复 worker 是否增加 attempt；增加则旧 candidate 被 discard，不增加则新 worker无法稳定复用旧 request_id/candidate_id。该 case 的生产时间线不可判定。 | 明确编排恢复时 attempt 的变化规则。若按新 attempt 重执行，则保留旧 candidate discard 规则，并把 CF-SER-11 改为验证“清空非 Ledger 缓存后，同 attempt 的 task_complete handler 可从 payload_ref 重建”，不要声称完整进程重启仍消费旧 candidate。 |
| R8-07 | MAJOR | §3.3 CandidateRecord；§5.1 SeriesCandidateSubmitRequest；§5.3 数据密级 | 新增 payload_ref 后未定义 `payload_digest` 是整条请求摘要还是 candidate_patches 摘要，也未规定其与 `payload_ref.digest` 的关系；payload artifact 的 kind、media_type、data_class 及 blob/index/CandidateRecord 的提交顺序亦未固定。实现可能无法一致进行重放比对、GC 保留和密级传播。 | 分别命名并定义 request digest 与 stored-payload digest；固定 payload artifact 的 kind/media_type/data_class，明确按 Blob 协议先落内容、再以单 Ledger 事务提交索引、CandidateRecord 和命令 receipt。 |
| R8-08 | NIT | 文档头部“关联” | 文本仍称“01 系五轮处置表”，括号中实际列出七轮计数 `57/47/34/21/18/9/9`。 | 改为“七轮处置表”。 |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

空；不重复登记 E-02。

## 7. 一句话总结

距离 draft freeze 仍需封闭授权恢复与 control-plane wire/幂等契约、统一 probe 丢失语义，并补齐 endpoint 与 candidate 恢复/持久化边界。

上面是Codex的review意见

全部材料核验完毕（v0.7→v0.8 diff、处置表、两份规范 YAML 复演、endpoint 文法逐条比对）。以下为第八轮评审。

---

# 复审报告：01_Contract_Spec v0.8（Kimi 方）

## 1. 总裁决：**GO**

本方第七轮唯一 NIT 妥善关闭；簇 IB 裁决我接受并己方复核错误；五个仲裁关注点全部接受。增量审查发现 1 条 MINOR（§5.1 与 §6 的用例覆盖声明不一致），作 freeze 勘误。

## 2. 任务 A：本方第七 issue 处置核验

| 编号 | 结论 | 说明 |
|------|------|------|
| M-01 (NIT) | 接受关闭 | heartbeat 不持久化 receipt（仅记最新 seq）、trace_append 免 receipt 以 (task, attempt, 首 seq) 天然去重——正是建议的特化处理，高频命令 receipt 膨胀问题解决 |

**对簇 IB 裁决的表态：接受裁决依据，承认我方复核有误。** 上轮 B-1 我写下"gate 值 {pass, approved, rejected} 全部落于注册集"——这不成立：approved/rejected 不在任何 agent 注册集内，我误把 gate outcome 纳入了注册集覆盖范围。裁决依据正确：§2.3 status_set 定义为 HandoffResult.status 词表，gate outcome 由框架合成（threshold 纯代码判定、hitl 经 Approval 决定+gate CAS）、从不经 HandoffResult 产出，因此不属于任何 agent 的 status_set，自证断言的正确量词是"agent 型来源对注册集、gate 型来源对 §1.2 固定集"。Codex 对，我误；量词修正后的断言形式良构。

## 3. 任务 B：仲裁关注点

**B-1（命令注册表与响应联合封闭完备）：接受。** 以写客户端标准核验：Envelope 增 command 判别后 payload 按命令名唯一解析，未知命令 E-CP-VALIDATION；注册表三列（命令名↔请求↔响应）覆盖全部 16 条命令，无专属响应者统一 CommandAck，错误统一 CommandError 封闭五码——broker/worker 客户端无猜测空间。AuthorizationConsumeResponse 三支判别联合对照 C-09/§2.2a 出口：granted=①核销+publishing；rejected 四 reason（authorization_expired/stale/revoked/already_consumed）覆盖②全部失效形态且补上双消费败者形态（CF-AUTH-02 的 loser 语义有了 wire 表示）；d12_mismatch=③。完备。

**B-2（probe 无回执两支）：接受。** 两条时间线走查：①"worker 每次回报前崩溃"——已 spawn（有启动记录）无 receipt → 合成 indeterminate receipt（origin=worker_loss）消耗该次 → 次消耗至 max → unknown+sys.error，有界收敛、终态唯一；②"编排 spawn 前反复崩溃"——pending 无 receipt 且无启动记录 → 重派同组不递增，幂等，假耗尽消除。两支以"worker 启动记录存在性"判定互斥，穷尽（spawn 与否二值）；上限不可突破（递增仅在新派发事务，合成消耗对应真实 spawn）。v0.7 修一头敞另一头的过失如实记录且修正正确——两支封闭后该协议我认为闭合。

**B-3（endpoint 文法 negative vectors）：接受。** 逐条可判定：尾点域名违 dns 规则、非压缩 IPv6 违 RFC 5952 压缩小写要求、前导零端口违端口规则、显式 :443 违"非 443 才出现"、点段路径违禁点段——每条 negative vector 命中唯一明确规则；"装载时校验、非 canonical 直接拒绝"消除实现自由度。`inprocess:<provider_id>` 为进程内 fixture 提供合法端点身份，D12 接合正确：inprocess↔https 切换改变 endpoint_identity → digest 失配 → administrative finalize，与冻结语义一致。

**B-4（payload_ref 链路）：接受。** 受理事务内将 candidate_patches 的 canonical 全文持久化为内容寻址 artifact → 分支 B 的 lineage/patch 应用/tree_digest 全部自 payload_ref 取原文 → 编排重启（CF-SER-11：进程内态清空后 task_complete 分支 B 走通）零依赖进程内态。与 CandidateRecord 其余字段（payload_digest 校验、request_id 幂等域）无冲突。

**B-5（量词修正后复演）：接受。** 旗舰：agent 来源 on（build_passed/build_failed_exhausted/ut_passed/ut_fix_patch_ready/bench_done/review_report_ready/release_plan_ready）逐项 ∈ 对应注册集 ✓；gate 来源 on（bench_gate:pass、human_review:approved/rejected）∈ §1.2 固定集 ✓。A：agent on（build_passed/bench_done）✓、gate pass ✓、terminal success_on=[pass] ⊆ review 集 ✓。两 case 同真，断言形式良构。

## 4. 新 issue 清单

| # | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|------|------|---------|-------------|
| M-01 | MINOR | §5.1 通用段 vs §6 CF-IPC-06 | §5.1 称"重放矩阵 CF-IPC-06 扩至含 reconciliation 与出向共五方向"，但 §6 的 CF-IPC-06 用例行仍为"worker/approval/broker 各一命令"三角色——注册表是 07 的判定，按 §6 文本实现的 harness 只覆盖三方向，reconciliation 与编排→broker 出向两个幂等域（本轮 R7-02 新增）无判定依据 | CF-IPC-06 行前置改为"五方向各一命令（worker/approval/broker/reconciliation/编排→broker 出向）"，期望列不变 |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

无新增。

## 7. 一句话总结

可以 freeze——01 八轮评审后命令面、幂等域、恢复协议、注册集自证全部闭合并经实证（golden vector 复算、双 YAML 逐名核对、两条崩溃时间线走查），建议将 M-01（CF-IPC-06 用例行同步五方向）作为 freeze 勘误随 v0.8 定稿。

上面是Kimi Code的review意见

CodingSystem 01_Contract_Spec v0.8 第八轮评审报告（评审员：Claude Code）
1. 总裁决：GO（无 BLOCKER/MAJOR/MINOR；2 NIT 随 GO 列为 freeze 勘误）
2. 任务 A：本方第七轮 issue 处置核验
第七轮编号	结论	说明
W7-01 (NIT)	接受关闭	§0.1 分型定案 StrEnum→字符串、IntEnum→整数（DataClass 即例，golden vector 的 allowed_max:1 成为其专属向量）——与本方复算结果一致；Codex 连异读 digest 都实算出来，同发现异评级的裁决无异议
C-vii (CLARIFY)	接受	endpoint 文法增 inprocess:<provider_id> scheme（进程内 fixture 专用），FakeLLMProvider 装配取值定案，且与 D12 复核正确接合（fake↔真实端点切换必失配）
（簇 IB 为 Kimi 表态项，本方无义务；附注：按 §2.3 status_set 定义裁 Codex 对的依据本方复核认同——status_set 是 HandoffResult.status 词表，gate outcome 由框架合成、不经 HandoffResult，v0.7 量词确有歧义。）

3. 任务 B：五个仲裁关注点
B-1（命令注册表与响应联合封闭完备）：接受，附 1 NIT。 以"写 broker/worker 客户端"标准过面：command 判别 + 封闭注册表（三列映射齐全，与 §5.1 各 socket 文字面逐条一致）、未知命令 E-CP-VALIDATION、无专属响应统一 CommandAck、错误封闭五码——每条命令请求/响应/错误唯一可解析。AuthorizationConsumeResponse 三支覆盖 C-09/§2.2a 全部出口：granted↔①、rejected(reason 四值，含并发双消费的 already_consumed，与 CF-AUTH-02"恰一成功"闭合)↔②、d12_mismatch↔③。NIT（X8-01）：D12 失配同时存在响应臂（d12_mismatch）与 CommandError 码（E-CP-D12-MISMATCH）两种表示，authorization_consume 走前者已由注册表确定，但双表示宜加一句注明分工。

B-2（probe 无回执两支）：接受。 两支以"worker 启动记录"（即 HLD §5.1.2 的 pgid 登记）判别，互斥完备；fork 后登记前崩溃的缝隙被判定序①兜住（僵尸 worker 迟到回报按同组 receipt 幂等/拒绝，无害）。两条时间线走查：worker 每次回报前崩溃 → 每次 spawn 消耗一次 attempt（合成 indeterminate 计入"含合成"的耗尽判定）→ 有界收敛至 unknown+sys.error，终态唯一；编排 spawn 前反复崩溃 → 同组重派不递增 → 无假耗尽，稳定后正常推进。上限在两条线上均不可突破。v0.7 修一头敞另一头的过失如实记录并正确闭合。

B-3（endpoint 词法文法 + negative vectors + inprocess）：接受。 五个 negative vector 各违反恰一条文法规则（尾点域名/非压缩 IPv6/前导零端口/显式 :443/点段路径），判定唯一；"非唯一表示一律拒绝"延续"规范化即校验"路线；inprocess scheme 进 digest 使 fixture/真实端点切换触发 D12 失配→C-14，语义正确。

B-4（payload_ref 链路）：接受。 受理事务持久化 canonical 全文为内容寻址 artifact，分支 B 的 lineage/应用/tree_digest 全部自 payload_ref 取原文；CF-SER-11 的"编排重启进程内态清空"用例断言零进程内依赖；payload_digest 与 payload_ref 同源一致。

B-5（量词修正后双 case 复演）：接受。 旗舰 agent 来源 on 五组（build_passed/build_failed_exhausted/ut_passed/ut_fix_patch_ready/bench_done/review_report_ready×2/release_plan_ready）逐项 ∈ 对应注册集；gate 来源（bench_gate pass、human_review approved/rejected）∈ §1.2 固定集；A4 的 agent on 与 terminal success_on=[pass]（review 集保留 pass）同样通过——CF-DSL-FLAG 与 CF-FIX-00 双 case 同真，量词修正消除了 v0.7 的歧义源。

4. 新 issue 清单（随 GO 的 freeze 勘误）
编号	等级	定位	问题描述	修改建议
X8-01	NIT	§5.1 CommandError vs AuthorizationConsumeResponse	D12 失配存在双表示：authorization_consume 的响应臂 d12_mismatch 与 CommandError 码 E-CP-D12-MISMATCH 并存，分工未注明	加一句：authorization_consume 的 D12 形态走响应联合；E-CP-D12-MISMATCH 仅用于其余 D12 复核点（dispatch/decision_submit），或从 CommandError 枚举中移除并逐点改用各自表示
X8-02	NIT	§4.3 合成 receipt	合成 indeterminate receipt（origin=worker_loss）的 payload digest 取值未定——判定序①对迟到真实回报做 digest 校验，需保证真实回报必不符从而被拒并审计，而非落入"返回首响应"	注明合成 receipt 的 payload_digest 为保留值（如全零），任何真实回报与之必不符 → 拒并审计
5. CLARIFY 清单
（空）

6. HLD 疑似缺陷登记
（空；E-02 候选已在册。）

7. 一句话总结
可以 freeze——第七轮 9 条全部闭合、五个仲裁关注点全过（含两条崩溃时间线与双 YAML 复演的独立走查），仅余 2 条注记级 NIT 随 freeze 编辑落入；本方连续第二轮 GO，支持在 Kimi/Codex 同轮转 GO 时宣布 01 draft freeze。

上面是Claude Code的review意见
