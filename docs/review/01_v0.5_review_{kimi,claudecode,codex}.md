CodingSystem 01_Contract_Spec v0.5 第五轮评审报告（评审员：Claude Code）
1. 总裁决：GO（无 BLOCKER/MAJOR；1 MINOR + 2 NIT 随 GO 列为 freeze 勘误）
本方第四轮 5 条全部关闭；六个仲裁关注点全部接受（一个附用例补行 MINOR）。两份规范 YAML 逐 token 复演通过。

2. 任务 A：本方第四轮 issue 处置核验
第四轮编号	结论	说明
Q-01 (MINOR)	接受关闭	§4.3 reconciliation 协议定案：专用 socketpair、命令面仅 probe_report 一条（封闭枚举不破，effect_claim/task_complete 显式不可达）、ProbeDispatch 消息定型
Q-02 (MINOR)	接受关闭	§0.2 decision/adjudication/correction_id 统一编排进程事务内生成，"与 token_id 同一单写者理由"——矛盾消除；DecisionSubmitCommand 两跳身份传递把 reviewer 的 peer 映射与单写者模型接合
Q-03 (MINOR)	接受关闭	CF-BLOB-05 读侧内容篡改 → digest 校验失败 → sys.error + 审计
Q-04 (NIT)	接受关闭	A0 补齐（ApprovalDecisionRequest/DecisionSubmitCommand·Result/ProbeDispatch/ProviderExecutionConfig 等全部新名）
Q-05 (NIT)	接受关闭	loader 步骤①明确"parse+compose 到节点树（不构造，标量原词素在节点层可得）"——词素时序歧义消除
3. 任务 B：六个仲裁关注点
B-1（resolution 表可实证性）：接受。 两份规范 YAML 逐 token 复演：旗舰——on: 键在显式表下非 true|false 必为字符串键；5.0 不匹配 int 正则、在节点层以原词素进入 value 位置处理转 "5.00"（与 §0.1 定点 pattern 无缝）；join:pre_final_review plain scalar 为单字符串；"<=" 引号字符串。A4 子集——terminal: true 命中 bool 白名单入 pydantic strict bool；success_on: [pass] 流式序列字符串；无 loops 节 → V-07 空覆盖合法；bench_gate fail → default_route 穷尽。010/1e3/八进制形字符串化规则虽未在两文件中出现但已封闭定义。解析且规范化唯一，字节断言可执行。显式表替代"YAML 1.2 core"名义是正确收紧——判定不再依赖外部规范解读。

B-2（task_complete 双分支与 candidate 链）：接受，附 1 MINOR。 分支判定以 HandoffResult.series_candidate_id 为唯一依据、封闭两支；candidate_id 由 series_candidate_submit 签发并绑定本 task、每 task 至多一、重复拒绝；他 task 引用/多 candidate → sys.invalid_result；分支 A 与 §3.3a 初始 series、publishing 降级（仅分支 B 的 series 步骤降级，结果照常）交互自洽——v0.2 起"无 patch 任务凭空推进 series"的存量缺陷根治。缺口：CF-SER 未落双分支专属用例（S-01）——分支 A"不动 series"断言与三类拒绝路径（他 task 引用/多 candidate/重复 submit）目前仅由 CF-FIX-02 间接沾边。

B-3（version CAS 全链）：接受。 claim 响应携 version → effect_transition (key, version, expected_state) 三元 CAS → probe_report (key, version, probe_attempt) CAS → 陈旧组拒绝并审计；与"abandoned 之上 version+1 新记录"的版本化通则闭环——CF-EF-09 的 v1 迟到 transition 不污染 v2 即该链的判定性证据；CF-EF-10 覆盖崩溃重放幂等。

B-4（ProviderExecutionConfig digest 域）：接受。 endpoint_identity/trust_policy/routing/allowed_max 入 digest、仅 secret 值排除——与 HLD D12"含策略语义与端点身份/信任策略（仅凭据值除外——密钥轮换不影响恢复，端点改向则触发不匹配）"逐要素对应；CF-SM-12 两用例（改向失配→C-14 / 轮换不失配）期望可判定。

B-5（reconciliation 事务点）：接受。 attempt 递增与 next_probe_at 置于派发事务（spawn 前）的保守语义正确——执行后/回写前崩溃该次已计数，probe_retry_max 上限物理不可突破，与框架 at-least-once 哲学一致；probe_report 三元 CAS 使三点崩溃注入（CF-EF-10）的期望逐点可判定；命令面封闭性经 B-2/Q-01 链条保持。

B-6（§6 + A4 终核）：接受。 逐 case 输入具体化完成（CF-DSL-03..08/14..17 每规则具体 fixture、CF-FIX 逐类展开含 candidate 分支 B/gateable 解析/V-13b 判定、CF-EF-06 拆静态与运行时）；两份规范 YAML 均随文入档、字节断言可执行；HLD §11.3 类别经四轮对账后零残漏（读侧损坏 CF-BLOB-05 本轮补上最后半格）。残漏仅 B-2 所指 CF-SER 双分支用例（同 S-01）。

4. 新 issue 清单（随 GO 的 freeze 勘误）
编号	等级	定位	问题描述	修改建议
S-01	MINOR	§6.4 CF-SER	task_complete 双分支（本轮 BLOCKER 级修复）无专属 conformance 用例：分支 A"仅提交结果、series/current/失效链全不动"断言，与三类拒绝（引用他 task candidate / 多 candidate / 重复 series_candidate_submit）均未落 case	增 CF-SER-07（分支 A：无 candidate 完成 → current 不变、无失效、结果照常）与 CF-SER-08（三类拒绝 → sys.invalid_result / 重复提交拒绝）
S-02	NIT	§4.2 迁移表	claimed/running → unknown 单元格写"对账：不可判定（重试耗尽）"，未注 §4.3 的 replay_safe fallback 例外（耗尽 ∧ replay_safe → abandoned 重放而非 unknown）——表与正文有半格漂移	单元格补"（且非 replay_safe fallback）"
S-03	NIT	§2.7 vs §7 A1	TraceEvent 双重定义：§2.7 具名模型与 A1 内联字典并存（payload 类型表述亦有微差），双源有漂移风险	删 A1 内联定义，仅留前向兼容语句并引 §2.7
5. CLARIFY 清单
（空）

6. HLD 疑似缺陷登记
（空——§4.3 的 indeterminate 耗尽后 replay_safe fallback 为 HLD 三支的能力组合细化，probe 优先原则保持，不构成偏离。）

7. 一句话总结
可以 freeze——四轮 BLOCKER 全部根治（含 v0.2 存量的 series 无条件推进 bug）、六个关注点终核通过、两份规范 YAML 字节断言可执行，仅余 1 条用例补行 MINOR 与 2 条表格/索引 NIT，建议随 freeze 编辑一并落入，无需再走完整复审轮。

上面是Claude Code的review意见

## 1. 总裁决：NO-GO

第四轮遗留的 CS-24、CS-25、R4-01、R4-03 仍含 BLOCKER 级缺口；此外新增 R5-01，为 01 遗漏 HLD 明文强制语义的 BLOCKER。

## 2. 任务 A 结果表

| 本方第四轮编号 | 结论 | 理由与定位 |
|---|---|---|
| CS-02 | 接受关闭 | §0.2 已对 `snapshot_id` 加全局唯一约束，并规定冲突时按 8 hex 逐次扩位，全量 digest 保留审计，唯一寻址问题已解决。 |
| CS-12 | 接受关闭 | §3.1 已内联 supersede 三条件和 CAS，§3.4 已内联 `max_rounds` 边界，§4.2 已列完整六态迁移边集；旧版本规范性外引已清除。模型精确性残留归 CS-24。 |
| CS-16 | 接受关闭 | §5.1 的 `DecisionSubmitCommand{request, reviewer}` 保留 Approval 服务认证出的 reviewer，`DecisionSubmitResult` 返回编排生成的 decision/token ID；审批两跳身份链已闭合。 |
| CS-24 | **不接受** | §0.4 声称“每个类型完整定义、无匿名 schema”，但 §2.4 的 `Finding`、`PendingEvent` 各分支和 `Producer` 等仍缺完整字段类型/判别模型；§5.1 多个控制面载荷仅为行内集合，A0 只是索引而非定义。无法直接生成完整 contracts 包。原 BLOCKER 维持。 |
| CS-25 | **不接受** | §6 仍非完全可判定：① CF-EF-02 仅列 `{可 probe,replay_safe,皆无}` 三类，却称覆盖四个能力组合；②其规定 indeterminate 耗尽一律 `unknown+sys.error`，与 §4.3 对 replay-safe 的 fallback 重放矛盾；③ CF-DSL-FLAG 所依赖的五类 Agent `status_set` 未在 CF-FIX 中给出；④ HLD §11.3 要求的“陈旧 series 进 gate”没有独立 case。原 BLOCKER 维持。 |
| R3-01 | 接受关闭 | §1.1 已改为 compose 节点树后显式解析，原词素在构造前取得；`on`、`true`、`010`、`5.0` 的目标类型明确。新增的一般化 loader 边界问题另见 R5-03。 |
| R3-03 | 接受关闭 | §4.1 与 CF-EF-05 已统一为：异参生成新键；同键 failed 才按 `failed_disposition` 处理。CF-EF-05/02 的用例轴完备性归 CS-25。 |
| R3-09 | 接受关闭 | reviewer 由 Approval 服务依据 CLI peer credential 生成，再通过可信 Approval socket 显式传给编排进程。 |
| R4-01 | **不接受** | §3.3 分支 A 的判据只是 `HandoffResult.series_candidate_id=None`，并未要求 Ledger 中不存在本 task candidate。反例：worker 已成功登记 candidate，但返回结果时漏填 ID，系统会静默走 A、丢弃 patch 并遗留 candidate。另有 ACK 丢失后“重复提交拒绝”与 §5.1 通用 request_id 幂等的冲突，candidate 也无 consumed/abandoned 状态。原 BLOCKER 维持。 |
| R4-02 | 接受关闭 | `effect_claim` 返回 version；`effect_transition` 使用 `(idem_key,version,expected_state)` CAS；`probe_report` 使用 `(idem_key,version,probe_attempt)` CAS；CF-EF-09 覆盖 v1 迟到污染 v2 的反例。 |
| R4-03 | **不接受** | §5.3 已纳入 endpoint/trust/routing/allowed_max，但 digest 算法仍不唯一：“排序列表”未定义排序键及 provider 唯一性；`endpoint_identity` 没有规范化算法；“排除 secret 值”没有类型化 secret 投影规则。不同实现可能对同一配置得到不同 digest，或漏掉端点变化。原 BLOCKER 维持。 |
| R4-04 | **不接受** | §4.3 的 `ProbeDispatch` 缺 `effect_class` 和 `probe(params, record)` 所需 record 视图，也未定义 worker 的只读取得路径；同时宣称“心跳语义同普通 worker”但专用 socket 又“仅开放 probe_report 一条命令”。重复 report 是返回首结果还是按“陈旧组拒绝”处理也与 CF-EF-10 的“幂等”不一致。原 MAJOR 维持。 |

## 3. 任务 B 结果

1. **Loader 显式 resolution 表：接受指定两份规范 YAML 的复演结果。**  
   旗舰和 A4 均可在节点层唯一处理：`on` 为字符串键；`join:pre_final_review` 为完整字符串；`5.0` 先保持词素，再变为 `"5.00"`；A4 的 `true` 为 bool；`010` 按表为字符串，在 strict 整数字段会拒绝而不会按八进制解释；标量 `to` 最终成为单元素列表。一般输入的引号和重复键边界另见 R5-03。

2. **task_complete 双分支与 candidate 链：反对。**  
   candidate ID 的基本生成、task 归属和单 candidate 限制已出现，但分支 A 未校验“零 candidate”，candidate 没有消费/退役状态，响应丢失后的安全重放也未定义；publishing 降级时 `candidate_ref` 与 candidate 状态的关系同样未闭合。CF-SER 也未覆盖“已登记 candidate 但结果传 None”和 candidate-submit ACK 丢失。

3. **effect/probe version CAS：接受。**  
   claim、普通 transition、probe report 三条路径均绑定足够的版本/CAS 字段；陈旧状态或版本只审计拒绝，CF-EF-09 能判定 v1 迟到不污染 v2。对账 worker 协议本身的问题不属于 CAS 身份链，见 B-5。

4. **ProviderExecutionConfig digest 域：反对。**  
   字段域方向忠实于 HLD D12，但排序、端点规范化、secret 排除投影没有唯一算法，CF-SM-12 也只写抽象“配置两变体”，不足以证明跨实现一致。

5. **Reconciliation 协议事务点：反对。**  
   spawn 前保守计数可以保证不突破上限，但专用通道缺 effect 类型/record 输入，heartbeat 与单命令封闭性冲突，`probe_retry_max` 是总尝试数还是重试数未定，ACK 丢失后的 report 重放结果也未闭合，三个 kill 点因此仍无法得到唯一期望。

6. **§6 + A4 conformance 权威：反对。**  
   A4 原文已经补入，但 CF-EF-02 与 §4.3 存在相反终态，CF-FIX 未给 CF-DSL-FLAG 所需的精确注册集合，陈旧 gate case 缺失，若直接编写 07 harness 仍需自行补充输入和选择期望。

## 4. 新 issue 清单

| 编号 | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|---|---|---|---|
| R5-01 | **BLOCKER** | §2.3 `HandoffResult`、§3.3 `task_complete`；HLD §4.2.4 | HLD 明文规定“HandoffResult 中的 series_id 经框架校验、不可自造”，但 01 只把它定义为任意 `str`，task_complete 两分支没有校验其等于本 dispatch 的输入 series，也没有要求各 Report 头与之相同。恶意或缺陷 worker 可把结果/Report 记到其他 series。属于 01 对冻结 HLD 的语义遗漏。 | 在 task_complete 两分支统一增加：连接推导的 task/dispatch 身份校验；HandoffResult.series_id 等于 TaskInput.series_id；所有 Report 的 run/task/series 与 Ledger 上下文一致；candidate.expected_parent 同样一致。不符统一 `sys.invalid_result`，并增加跨 series 伪造 case。 |
| R5-02 | MAJOR | §0.2、§2.1 C-11、§5.1 `adjudicate` | §0.2 规定 `adjudication_id` 由编排事务生成、请求不含、响应返回；§5.1 却要求请求携带 `adjudication_id`，C-11/§2.2c 又以该 ID 做幂等。生成方、请求模型和重放键无法同时成立。 | 定义具名 AdjudicateRequest/Result：请求使用独立 request_id，编排事务生成并返回 adjudication_id；或明确改为调用方生成并同步修改 §0.2。只能保留一种方案。 |
| R5-03 | MAJOR | §1.1 loader resolution 表 | 新 resolution 表未说明规则是否只作用于 plain scalar。反例：`terminal: "true"` 可被实现 A 重解析为 bool，也可被实现 B 保持字符串并在 strict 校验中拒绝；引号空串与 null 同理。重复 mapping key/NFC 后重复键的 loader 处置也未定义；compose 后无引用的 anchor 信息可能已丢失，无法按现有步骤可靠拒绝。 | 明确 plain、未显式 tag 的 scalar 才参与 bool/int/null 解析，所有 quoted/block scalar 恒为字符串；在 compose 前的 token/event 阶段拒绝 anchor/alias/merge/custom tag；mapping 构造前对原键及 NFC 后键做重复检测并拒绝。 |
| R5-04 | NIT | §0.3、文档头部/A3 | 头部称“01 系三轮处置表”却列出四轮计数；§0.3 仍称“文档展示号 v0.2”。 | 改为“四轮处置表”，并把展示号表述改成当前 v0.5 或与具体草案号无关的通用描述。 |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

空。本轮 HLD 对照发现的是 01 漏落 HLD 已定语义，不是 HLD 本身缺陷。

## 7. 一句话总结

距离 draft freeze 仍需关闭 candidate 分支判据、完整 schema/conformance、Provider digest、reconciliation 协议及 HandoffResult series 防伪等 BLOCKER/MAJOR，当前不可 freeze。

上面是Codex的review意见

第四轮处置表与 v0.5 全文（含附录 A4）已核对完毕，两份规范 YAML 已在显式 resolution 表下逐 token 复演。以下为第五轮评审。

---

# 复审报告：01_Contract_Spec v0.5（Kimi 方）

## 1. 总裁决：**GO**

本方第四轮 3 条 issue + 1 条 CLARIFY 全部妥善关闭；六个仲裁关注点逐一复演后均接受。增量审查发现 2 条 MINOR + 1 条 NIT，作 freeze 勘误。R4-01（无 patch 任务凭空推进 series）的修复方向核验正确——这是本流程迄今最有价值的一次存量 bug 捕获。

## 2. 任务 A：本方第四轮 issue 处置核验

| 编号 | 结论 | 说明 |
|------|------|------|
| M-01 | 接受关闭 | 簇 FA：§4.2 六态迁移表已内联为完整表格（全边集 + 两处修正转表内语义），§3.1 supersede 三条件与 §3.4 max_rounds 一并内联复述并标权威——引违例清零 |
| M-02 | 接受关闭 | loader 改显式 resolution 表：bool 仅 `true\|false`（`terminal: true` 正常得布尔，`on:` 键得字符串）——粒度问题被更彻底的方案消解（不再借"YAML 1.2 core"名义指代，`010` 实测反驳亦在内） |
| M-03 | 接受关闭 | §2.7 全模型字段级定义落地，命名统一为 CreateRunRequest·Response（无 Skill 前缀），A0 同步 |
| CL-1 | 接受关闭 | 簇 FB：reconciliation 协议定案——专用 socketpair、命令面仅 probe_report 一条（封闭枚举不被突破）、ProbeDispatch 消息齐备、命令适用面疑问全部解答 |

## 3. 任务 B：仲裁关注点

**B-1（loader 显式 resolution 表）：接受。** 两份规范 YAML 逐 token 复演：`on:` 键——非 bool（表内 bool 仅 true|false）、非 int、非 null → 字符串 ✓；`value: 5.0`——节点层取原词素 "5.0"，十进制词素校验通过、scale 补零得 "5.00"，不经浮点解析 ✓；A4 的 `terminal: true`——表内 bool ✓，pydantic strict 收原生 bool ✓；`010` 类词素——int 正则 `^-?(0|[1-9][0-9]*)$` 不匹配 → 字符串（规则明确无歧义）✓；`join:pre_final_review` 冒号后随非空格 → 完整字符串 ✓；`1e3` 归"其余标量一律字符串"（resolution 表是全函数，无未分类词素）✓；`dsl_version: 1` 经 int 正则得原生 int，Literal[1] strict 通过 ✓。表→pydantic strict 接合无缝（解析产物均为原生类型），规范化唯一性由"全函数 + 词素级处理"保证。

**B-2（task_complete 双分支 + candidate 引用链）：接受。** 引用链闭合：series_candidate_submit 返回 candidate_id（ulid、绑定本 task、每 task 至多一、重复拒绝）→ HandoffResult.series_candidate_id 引用 → 分支判定封闭（None=分支 A 只提交结果不动 series——UT/Review 常态，v0.2 存量 bug 根除；恰一自有 candidate=分支 B 全步；他 task 或多 candidate → sys.invalid_result）。publishing 交互：分支 B 步 1 降级 pending、分支 A 无 series 步骤天然安全，与"结果与 artifact 照常提交"一致。CF-SER-03/CF-FIX-02 对应到位。一处表格缺漏见 M-01（NIT）。

**B-3（version CAS 全链）：接受。** claim 的 execute 响应携带 version effect_transition 三元 CAS（idem_key, version, expected_state）→ probe_report 三元 CAS（idem_key, version, probe_attempt）→ 陈旧一律拒绝并审计。CF-EF-09 场景（v1 abandoned、v2 running、v1 迟到 succeeded transition 被拒、v2 不受污染）正是版本化通则与 CAS 的接缝要害，断言正确。全链闭合。

**B-4（ProviderExecutionConfig digest 域）：接受。** 逐字段对 HLD D12（冻结版）：endpoint_identity（规范化端点、凭据值除外）+ trust_policy 对应"端点身份/信任策略"；routing（模型绑定/cascade）+ allowed_max + provider_id/type 对应"策略语义"；仅 secret 值排除对应"仅凭据值除外"。CF-SM-12 双用例（端点改向→失配→C-14；密钥轮换→不失配）与 HLD 文句逐字对应，判定无歧义。忠实。

**B-5（reconciliation 协议事务点）：接受。** 派发事务内递增 probe_attempts + 写 next_probe_at（spawn 前）的保守语义成立：worker 执行后/回写前崩溃该次已计数——上限永不突破，代价是崩溃循环下可能"未执行即耗尽"→ unknown + sys.error，属 fail-closed 方向的正确取舍且文本明示"保守"；probe_report CAS 三元拒陈旧并审计；命令面仅 probe_report 一条，数据面封闭枚举不被突破（我第四轮 CL-1 的解答形态）；三点崩溃注入（CF-EF-10）期望"上限不破 重放同组幂等"可机械判定。闭合。

**B-6（§6 + A4 终核）：接受。** 逐 case 输入具体性：CF-DSL-03..08/14..17 全部给出具体 fixture（如 06a 为"default_route 删行"），CF-FIX-01..05 逐类展开到 produce/consume 清单；A4 收录子集规范原文并逐条复核通过（无环 V-07 空覆盖、bench_gate 支配链、terminal success_on 无出边、"status 名依 CF-FIX fixture 注册集"注记闭合了 fixture 依赖）；CF-DSL-FLAG 对两份原文字节的断言可执行。HLD §11.3 类别逐类对账：零残漏（新增 CFLOB-05/CF-SM-12/CF-EF-09/10 与 CF-DSL-PROP 的 V-07/V-13 性质亦已落入）。

## 4. 新 issue 清单

| # | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|------|------|---------|-------------|
| M-01 | MINOR | §6.3 CF-EF-02 vs §4.3 | CF-EF-02 的轴仍是三分类 {可 probe, replay_safe, 无}，未覆盖"可 probe ∧ replay_safe"第四组合，且其单元格规则"indeterminate→重试耗尽后 unknown+sys.error"与 §4.3 新增 fallback（indeterminate 耗尽后 **若 replay_safe → 退役 abandoned 版本化重放**）直接矛盾——§4.3 自称"四组合全覆盖 = CF-EF-02 轴定案"，但 CF-EF-02 文本未同步，harness 按 CF-EF-02 写将判错该组合 | CF-EF-02 轴改 supports_probe × replay_safe 四组合，单元格规则同步 fallback 支（耗尽∧replay_safe→abandoned+版本化重放；耗尽∧replay_safe→unknown+sys.error） |
| M-02 | MINOR | §5.1 vs §0.2 | candidate_id（series_candidate_submit 返回、HandoffResult 引用、分支 B 判定键）未入 §0.2 标识符总表——格式（ulid，§5.1 行文）、生成方（编排，series_candidate_submit 事务）、唯一性域（run 内？task 内？）三要素均需查两处推断；该表历来是 ID 问题的终审依据 | §0.2 补 candidate_id 行：ulid ｜ 编排（series_candidate_submit 事务）｜ task 内唯一（每 task 至多一） |
| M-03 | NIT | §2.1 末段 §5.1 | pending_register 载荷签名两说：§2.1 末段仍写 `{kind, dedup_key}`（v0.4 前形态），§5.1 已定案 `{run_id, event: PendingEvent}`（判别联合整体提交，dedup_key 为联合公共字段）——CS-10 闭合只改了 §5.1 | §2.1 末段签名同步为 §5.1 形态（或注明其为摘要式写法并引 §5.1） |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

1. HLD §5.2（冻结版）异参新键枚举中"干净终态 failed 依既有**类别策略**放行"一语两读：①准入受 failed_disposition辖（v0.4 CF-EF-05 旧读法）；②"放行"为定论、类别策略仅辖旧记录处置（01 v0.5 采读法②，且读法①与 idem_key"异=新逻辑效应"的定义相矛盾——对异参新键执行旧记录"版本化重试至上限"无语义）。01 的选择自洽且为唯一不矛盾读法，但 HLD 措辞本身是歧义源，登记备 HLD 下次修订时澄清（不影响本轮裁决）。

## 7. 一句话总结

可以 freeze——五轮评审后 01 的 loader、series、效应、发布链、conformance 五线与自足性全部闭合，建议将 M-01～M-03 作为 freeze 勘随 v0.5 定稿（M-01 优先，属注册表与正文的直接矛盾）。

上面是Kimi Code的review意见
