# 01_Contract_Spec v0.8 三方复审处置表（→ v0.9）

裁决输入：Kimi **GO**（1 MINOR；对簇 IB 裁决正面接受并承认复核有误）、Claude Code **GO**（连续第二轮，2 NIT）、Codex NO-GO（维持 5 + R8-01..08：4 BLOCKER + 3 MAJOR + 1 NIT）。合计 12 个独立问题，**全部采纳**（一处取 Codex 明示可接受的中间形态，注明）。

## 交叉簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| JA 合成 receipt 缺模型/保留 digest | CC X8-02(NIT); Codex R8-04(BLOCKER) | ProbeReceipt 具名模型（origin 三值：worker / synthetic_spawn_failed / synthetic_worker_loss；合成项 payload_digest 保留值全零，真实迟到回报必不符→拒并审计）；三态定案（未尝试重派不递增 / spawn 明确失败合成消耗 / 已启动后丢失合成消耗）；**耗尽后严格走 §4.3 能力矩阵**（v0.8"无条件 unknown"与 CF-EF-02 矛盾属实）；CF-EF-10 行两终态分行 |
| JB D12 双表示 | CC X8-01(NIT); Codex R8-02 部分 | 分工定案：authorization_consume 仅走响应臂 d12_mismatch；CommandError 的 E-CP-D12-MISMATCH 仅用于 decision_submit/release_dispatch 等其余复核点（CC 方案 A） |

## Codex——全采纳
R8-01 BLOCKER → C-09 guard 扩 publishing 恢复臂：consumed_by==本 task ∧ 绑定匹配 → `resumed`（不重复核销、不迁移）；他 task → rejected(already_consumed) fail-closed；§2.2a 四支、响应联合四臂、CF-AUTH-05 同步——HLD §8.3 明文形态在 wire 上无出口，成立。R8-02 BLOCKER → CommandName StrEnum（17 命令）+ 无省略三列注册表 + CommandAck/CommandError 具名 + CommandResponse 判别 envelope + 簇 JB。R8-03 BLOCKER → **取其明示可接受的中间形态**：幂等语义无豁免（heartbeat/trace 保持 scoped request_id + digest 检测），仅存储特化为 (task, attempt) 滚动窗口 K≥64、保留期 Run 存续——Kimi M-01 的膨胀关切与 HLD §8.5 无例外同时满足，不动 HLD；出向 release_dispatch 持久幂等由授权记录状态机承载（granted/resumed/rejected 三臂），broker 不持久化 receipt（与 R8-01 咬合）。R8-04 BLOCKER → 簇 JA。R8-05 → endpoint 自足文法（label 正则与长度、IPv4 段域、端口 1–65535、path 字符域、inprocess id 必等 provider_id）+ CF-SM-13 边界 vectors。R8-06 → 编排重启语义定案（HLD §5.1.2/§4.1：worker EOF 自杀→新 attempt→旧 candidate discarded）；CF-SER-11 按其建议改写为"同 attempt 内非 Ledger 缓存清空后自 Ledger 重建"，重启路径归 CF-SER-10——v0.8 的 CF-SER-11 与 discard 规则自相矛盾属实。R8-07 → payload artifact 三元组（kind/media_type/data_class=internal_code）+ payload_digest≡payload_ref.digest + request_digest 分立 + Blob 先落再单事务提交序。R8-08 → 头部轮数更正。
维持项 CS-24/R6-02/R7-02 → R8-02 终解；R6-01 → R8-03；R4-04/R6-06/R7-03 → 簇 JA；R4-03/R6-05/R7-04 → R8-05。

## Kimi / Claude Code——全采纳
Kimi M-01 → CF-IPC-06 前置改五方向并补出向三臂与 heartbeat 窗口内外期望。CC X8-01 → 簇 JB；X8-02 → 簇 JA。

## 汇总
- 12 条全采纳；R8-03 的仲裁为唯一非字面采纳（语义保留、存储特化），依据是 Codex 原文"可对高频命令采用压缩/滚动保存但不能取消异载荷检测"。
- v0.9 复审重点：(1) C-09 四支 + resumed 臂在 broker 恢复全时间线上的闭合；(2) CommandName/注册表/CommandResponse 三件套零省略零歧义；(3) 滚动窗口幂等是否满足 HLD §8.5 字面（关键表态项：三方对"语义保留、存储特化不构成豁免"各表一次态）；(4) ProbeReceipt 三态 × 两能力终态矩阵；(5) endpoint 文法 CF-SM-13 与编排重启定案后 CF-SER-10/11 分工。
