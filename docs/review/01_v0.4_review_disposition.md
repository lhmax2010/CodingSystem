# 01_Contract_Spec v0.4 三方复审处置表（→ v0.5）

裁决输入：Kimi **GO**（2 MINOR + 1 NIT + 1 CLARIFY）、Claude Code **GO**（3 MINOR + 2 NIT）、Codex NO-GO（重开 8 + 3 BLOCKER + 1 MAJOR）。合计 21 条，**全部采纳**。probe 隔离新方案（reconciliation worker）三方一致确认成立、正式关闭。

## 共识/交叉簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| FA 迁移表外引（自足声明自我违反） | Kimi M-01; Codex CS-12 部分 | §4.2 内联完整六态迁移表（全边集表格化，两处修正转为表内语义）；§3.1 supersede 三条件与 §3.4 max_rounds 语义一并内联复述（权威标注 HLD——引用冻结 HLD 本属合法，内联以息争） |
| FB reconciliation 通道/协议缺口 | CC Q-01; Kimi CL-1; Codex R4-04 | §4.3 定案：专用 socketpair、命令面仅 probe_report 一条（封闭枚举不破）；ProbeDispatch{idem_key,version,probe_attempt,params_ref}；attempt 递增与 next_probe_at 在派发事务（spawn 前）——保守计数上限不破；(key,version,attempt) CAS 拒陈旧；三点崩溃注入 CF-EF-10 |
| FC decision_id 生成方矛盾 + reviewer 两跳丢失 | CC Q-02; Codex CS-16/R3-09 不接受 | §0.2 decision/adjudication/correction_id 统一编排事务内生成；approval socket 改内部命令 DecisionSubmitCommand{request, reviewer}（Approval 按 CLI peer credential 填 reviewer，编排信任 approval socket——Approval 属框架 TCB）；响应 DecisionSubmitResult{decision_id, token_id|None} 具名 |

## Codex（重开 8 + R4-01..04）——全采纳

| # | 处置 | 落点 |
|---|------|------|
| R4-01 BLOCKER | 采纳 | task_complete 拆两条封闭分支：无 candidate 只提交结果不动 series（UT/Review 常态）；恰一 candidate（HandoffResult.series_candidate_id 引用、每 task 至多一、candidate_id 由 series_candidate_submit 返回）走全步；他 task/多 candidate → sys.invalid_result。**其反例揭示的是 v0.2 起的真 bug**（无 patch 任务凭空推进 series），三家前三轮均未抓到 |
| R4-02 BLOCKER | 采纳 | effect_transition 带 version+expected_state 三元 CAS；probe_report 同（簇 FB）；CF-EF-09 并发用例 |
| R4-03 BLOCKER | 采纳 | ProviderExecutionConfig 模型定 digest 域（endpoint_identity/trust_policy/routing/allowed_max，仅 secret 值排除）；CF-SM-12 端点改向失配/密钥轮换不失配双用例 |
| R4-04 | 采纳 | 簇 FB |
| CS-02 | 采纳 | snapshot_id 唯一约束 + 确定性扩位（+8 hex 逐次） |
| CS-12/CS-24 | 采纳 | 簇 FA + §2.7 全模型字段级定义（PatchItem/CreateRun/SubmitPatch/RunStatus+SnapshotInfo/Cancel/TraceEvent）+ SkillCreateRunRequest 命名统一（Kimi M-03 同） |
| CS-16 | 采纳 | 簇 FC |
| CS-25 | 采纳 | CF-DSL-03..08 与 14..17 逐行化（每 rule 具体 fixture）；CF-FIX-01..05 逐类展开；CF-DSL-PROP 补 V-07/V-13 性质；CF-EF-06 拆静态/运行时两行；**A4 附 compiler_bench_only.yaml 规范原文**（"未随文提供"批评成立）；CF-EF-02 轴定案四组合（§4.3 补 probe 耗尽后 replay_safe fallback 支） |
| R3-01 | 采纳 | loader 改显式 resolution 表（不再以"YAML 1.2 core"名义指代——其 `010` 实测反驳成立）：compose 节点层词素、bool 仅 true|false、int 仅无前导零十进制、其余字符串；Kimi M-02 的 true/false 粒度与 CC Q-05 的节点层时序同解 |
| R3-03 | 采纳 | failed 异参终修：异参一律放行新键（新逻辑效应，准入不受 disposition 辖——HLD"放行"字面）；同键才按 disposition；CF-EF-05 单元格与 §4.1 正文同步，与 idem_key 定义的矛盾消除 |

## Kimi / Claude Code 其余（全采纳）
Kimi M-01→簇 FA；M-02→loader resolution 表（true/false 保留为 bool）；M-03→命名统一；CL-1→簇 FB。
CC Q-01→簇 FB；Q-02→簇 FC；Q-03→CF-BLOB-05 内容篡改读侧用例；Q-04→A0 补条目（随 FC 改为 DecisionSubmitCommand·Result）；Q-05→节点层词素注明。

## 汇总
- 21 条全采纳；R4-01 值得标注：三轮 57+47+34 条评审未覆盖的 v0.2 存量 bug 在第四轮被构造出来——"不为凑数造 issue"纪律下的 NO-GO 方仍在产出实质发现。
- v0.5 复审重点：(1) loader resolution 表在两份规范 YAML（旗舰 + A4 子集）上的可实证性；(2) task_complete 双分支与 candidate 引用链；(3) effect/probe 的 version CAS 全链；(4) ProviderExecutionConfig digest 域；(5) reconciliation 协议事务点；(6) §6 与 A4 补全后的终核。
