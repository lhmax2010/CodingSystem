# 01_Contract_Spec v0.3 三方复审处置表（→ v0.4）

裁决输入：Kimi **GO**（3 MINOR）、Claude Code **GO**（3 MINOR + 2 NIT + 1 CLARIFY）、Codex NO-GO（重开 11 + 7 BLOCKER + 2 MAJOR + 1 MINOR + 1 NIT）。合计 34 条，**全部采纳**。
**任务 D（HLD 勘误回写核验）：三方四项全部通过**——两家以 git diff 实证改动仅头部一行 + A15，程序/文本/连带四维零发现。E-01 链条就此闭合，本轮起不再列任务项。

## 共识/交叉簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| EA SER-0 格式特例 | Kimi M-01; CC R-03; Codex R3-05 | §3.3a 清除字面特例：初始 series 按 §0.2 正常格式签发，行文不再用 SER-0；CF-SER-06 同步 |
| EB ReleasePlan 代码块未同步 | Kimi M-02; CC P-06 不接受/R-01; Codex R2-04/CS-16 部分 | §2.5 改 `class ReleasePlan(ReportBase)`，删重复字段——v0.3 处置半途（声明改了代码块没改），本表如实记录处置不彻底 |
| EC 自足性（外引/匿名载荷/聚合 case） | Codex CS-12/CS-24/CS-25 不接受; CC R-05 | §1.2/§3.2/§3.6/§4.1 全部内联为自足文本（JsonValue 一并定义）；§2.7 全端点具名模型；§6 聚合行展开为逐 case（V-01/V-10/V-13 独立 case 补齐，CF-AUTH/IPC/BLOB/SKILL/DC 逐行化）；"无匿名 schema"承诺至此兑现 |

## Codex（重开 11 + R3-01..R3-11）——全采纳
R3-01 BLOCKER → §1.1 loader 定案（YAML 1.2 core 语义 / SafeLoader 移除 bool·timestamp resolver、`on` 必为字符串键、预处理四步序、CF-DSL-FLAG 原文字节断言）——其 PyYAML 1.1 布尔坑实证成立，v0.3 wire 修复漏了 loader 本身。R3-02 BLOCKER → RetryOutcome 三值封闭枚举（invalid_result 可重试为 v0.2 起引入的 HLD 违反，重开成立）。R3-03 BLOCKER → CF-EF-05 failed 单元格按 disposition 展开（无条件放行与 HLD"依类别策略"冲突属实）。R3-04 BLOCKER → SubmitPatchResponse 补 queue_position（重放返回当前位置并注明）。R3-05 BLOCKER → 簇 EA。R3-06 BLOCKER → Producer 判别联合（AgentProducer/ExternalProducer，外部 Coding Agent 归 principal）。R3-07 BLOCKER → 逐字符唯一转义表 + 边界 golden vectors。R3-08 MAJOR → probe 迁入专用 reconciliation worker（无 Ledger 写权限，业务插件不入单写者进程——其隔离论证成立，v0.3"编排进程执行"定案推翻）。R3-09 MAJOR → reviewer 移出请求、peer credential 映射生成。R3-10 → origin_node 语义注明。R3-11 → 计数措辞更正（实为 14 项重开，v0.2 表标题笔误，本表留痕）。
重开残留逐条：CS-01→R3-07；CS-02→token_id 改编排事务内生成（Approval 不产 ID，载荷矛盾消解）+ snapshot 引用语义句；CS-04→R3-01；CS-10→pending_register 提交 PendingEvent 联合整体；CS-12/24/25→簇 EC；CS-16→token_id 生成方 + 簇 EB；CS-19→EffectRecord 补 probe_attempts/next_probe_at + EffectError 封闭归类；CS-23→ToolFact 带 data_class、SourceExcerpt 恒 internal_code、facts 派生规则。

## Kimi（M-01..M-03）/ Claude Code（R-01..R-05 + C-i）——全采纳
Kimi M-03 → effect_claim 服务端交叉校验 effect_class（不符 sys.invalid_result + 审计）。CC R-02 → decision_submit{ApprovalDecisionRequest}；R-04 → CF-ACT-08 stale 到达 case；R-05 → 簇 EC；C-i → 二进制 patch v1 显式拒绝（422）。R2-02 残留（Codex 任务 A 指出）→ V-02 attestation 三重绑定（制品 digest + contract_version + 套件版本）。

## 汇总
- 34 条全采纳；一处定案推翻（probe 执行进程：编排进程 → reconciliation worker，依 Codex 隔离论证）；两处处置诚实性记录（EB 半途处置、R3-11 计数笔误）。
- v0.4 复审重点：(1) loader 定案后 CF-DSL-FLAG 的原文字节可实证性；(2) 自足性核验（全文无外引/匿名载荷残留）；(3) reconciliation worker 隔离与对账协议的接合；(4) token_id/reviewer 生成方修正后审批链载荷闭环；(5) §6 展开后逐 case 可判定性终核。
