# 01_Contract_Spec v0.6 三方复审处置表（→ v0.7）

裁决输入：Kimi NO-GO（仅 1 BLOCKER I-01，其余全接受）、Claude Code NO-GO（1 MAJOR U-01 + 1 MINOR U-02，其余全接受）、Codex NO-GO（维持 5 + R6-01..06：4 BLOCKER + 2 MAJOR）。合计 9 个独立问题，**全部采纳**。

## 共识簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| HA CF-FIX-00 注册集与旗舰原文不符（**三家同发现**：build_failed_exhausted / review_report_ready / release_plan_ready 三名缺失） | Kimi I-01(BLOCKER); CC U-01(MAJOR); Codex R6-04(BLOCKER) | 注册集逐名对齐旗舰原文（旗舰属 HLD 冻结文本不可动，只改注册集）；review 集保留 pass 供 A4 terminal；case 期望改为"harness 从两份原文字节抽取 on/success_on 逐项断言 ∈ 注册集"——注册集错误从此自证红灯。作者过失如实记录：v0.6 定集时未回对旗舰原文 |
| HB candidate 跨 attempt 恢复自锁 | CC U-02(MINOR); Codex R6-03(BLOCKER) | CandidateRecord 完整模型（含 attempt/request_id/payload_digest/state）；"至多一活跃"限当前 attempt；attempt 启动事务原子 discard 旧项且不阻塞新登记；幂等域 = (task, attempt, 命令)——跨 attempt 同 request_id 为新提交（CC 方案一，与 Codex 的 attempt 入域建议同构）；CF-SER-10 恢复链 + kill 点 |

## Codex 其余——全采纳
R6-01 BLOCKER → §5.1 统一 CommandEnvelope{request_id, payload}：分角色幂等域（worker=(task,attempt,命令)）、保留期、receipt 持久化（同载荷重放返回首响应、异载荷 E-CP-IDEM-MISMATCH）、CF-IPC-06 三组重放矩阵——HLD §8.5"命令幂等"遗漏成立。R6-02 BLOCKER → SuiteResult/MetricEntry/RoundDigest/ReportEntry/EffectError 全模型化 + §5.1 命令载荷模型块（Envelope/Claim 三型响应联合/Transition/Blob 三命令/Candidate/TaskComplete/DecisionSubmit/Adjudicate/ProbeDispatch·Report/RecordView/ReleaseDispatch 等全字段类型）+ A0↔正文一一解析的 CI 自动检查约定。R6-05 → provider_id ASCII 文法 + UTF-8 字节序、endpoint 封闭文法（装载时校验而非运行时变换——"规范化即校验"消除实现自由度）、模型声明无 secret 字段（投影规则留作通则）、**golden vector 实算入档**（输入 + sha256:fe4e…）。R6-06 → receipt 持久化判定序（同组 receipt 优先于过期判定、payload digest 校验）、pending 重派同组不递增（spawn 前崩溃不假耗尽——v0.6"保守计数"语义修正为"计数=派发事务、重派幂等"）、CF-EF-10 逐点断言细化。
维持项 CS-24/CS-25 → 随 R6-02/HA 处置（自足声明的 CI 自动检查为终解）。

## Kimi / Claude Code 其余——全采纳
Kimi I-01 → 簇 HA。CC U-01 → 簇 HA；U-02 → 簇 HB。

## 汇总
- 9 条全采纳；一处语义修正如实标注（probe 计数从"保守可假耗尽"改为"派发事务计数 + pending 重派幂等"，消除 spawn 崩溃假耗尽——Codex R6-06 论证优于 v0.6 取舍）。
- v0.7 复审重点：(1) 注册集对齐后 CF-DSL-FLAG/CF-FIX-00 双 case 同真可实证；(2) CommandEnvelope 分角色幂等域与既有 Skill request_id 的并立清晰性；(3) CandidateRecord 恢复链（CF-SER-10）；(4) probe receipt 判定序三支的互斥完备；(5) provider golden vector 复算一致。
