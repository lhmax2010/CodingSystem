# 01_Contract_Spec v0.5 三方复审处置表（→ v0.6）

裁决输入：Kimi **GO**（2 MINOR + 1 NIT + 1 条 HLD 措辞歧义登记）、Claude Code **GO**（1 MINOR + 2 NIT）、Codex NO-GO（维持 5 + 新增 1 BLOCKER + 2 MAJOR + 1 NIT）。合计 18 条，**全部采纳**。

## 交叉簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| GA CF-EF-02 与 §4.3 fallback 漂移 | Kimi M-01; CC S-02; Codex CS-25①② | CF-EF-02 轴改 supports_probe × replay_safe 四组合、单元格补 fallback 支；§4.2 迁移表 unknown 单元格补"∧非 replay_safe fallback"注——三家从注册表/迁移表/正文三个面命中同一处 v0.5 未同步 |
| GB candidate 链完备性 | Codex R4-01 维持; CC S-01 | 分支判定改 **Ledger 在册数对账**（在册1引用None=漏填拒绝、在册0引用非None拒绝——"静默丢 patch"反例封死）；candidate 状态机 registered/consumed/discarded + attempt 启动对账退役；submit 幂等按 request_id、异载荷二次拒绝；CF-SER-07/08 专属用例 |

## Codex——全采纳
R5-01 BLOCKER → §3.3 统一前置校验（socket 推导身份、HandoffResult.series_id == TaskInput.series_id、Report 头逐项一致、candidate.expected_parent 一致，违者 sys.invalid_result；CF-SER-09 跨 series 伪造用例）——HLD"不可自造"字面的落地遗漏，成立。R5-02 → AdjudicateRequest/Result 具名 + adjudication_id 编排生成 + request_id 幂等（§0.2/§2.2c/§5.1 三处同步——v0.5 K4 只改了一处，处置不彻底如实记录）。R5-03 → resolution 表限 plain scalar（quoted/block 恒字符串）、anchor/alias/merge 在 parse/event 阶段拒、mapping 构造前原键+NFC 键重复检测。R5-04 → 头部轮数与 §0.3 陈旧展示号更正。
维持项：CS-24 → Finding/PendingEvent 四型/Producer 完整判别模型落码块（控制面载荷具名化随 AdjudicateRequest 等一并推进）。CS-25 → 簇 GA + CF-FIX-00 注册集定案（两份规范 YAML 的 on/success_on 全落于集内）+ CF-AUTH-07 陈旧 series 进 gate 独立 case。R4-01 → 簇 GB。R4-03 → digest 唯一算法（provider_id 排序键+唯一约束、endpoint 规范化算法、SecretStr 类型化投影占位）。R4-04 → ProbeDispatch 补 effect_class/record_view、通道两命令（heartbeat 保留——v0.5"仅一条"表述修正）、probe_retry_max=总尝试数含首次、同组 report 重放幂等返回首结果（过期组才拒）。

## Kimi / Claude Code——全采纳
Kimi M-01→簇 GA；M-02→§0.2 补 candidate_id 行；M-03→§2.1 pending 签名同步 §5.1。CC S-01→簇 GB 用例；S-02→簇 GA；S-03→A1 TraceEvent 双定义清除（仅留 §2.7）。
**Kimi HLD 登记（记录不动作）**：HLD §5.2"干净终态 failed 依既有类别策略放行"存在两读，01 采唯一自洽读法（异参放行、同键按 disposition）；登记为 HLD 下次修订的澄清候选（E-02 候选），不触发本轮勘误通道。

## 汇总
- 18 条全采纳；两处 v0.5 处置不彻底如实记录（R5-02 的三处未同步、R4-04 的"仅一条命令"与 heartbeat 冲突）。
- v0.6 复审重点：(1) 统一前置校验 + 在册数对账后 series 防伪与 candidate 链终核；(2) resolution 表 plain 作用域下两份规范 YAML 复演不变性；(3) provider digest 唯一算法可实证；(4) reconciliation 补全后三点注入期望唯一性；(5) CF-FIX-00 注册集与 CF-DSL-FLAG 的接合。
