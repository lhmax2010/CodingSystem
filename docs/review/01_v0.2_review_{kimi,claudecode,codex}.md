## 1. 总裁决：NO-GO

仍存在多项 BLOCKER，包括冻结 HLD 旗舰 DSL 无法按 v0.2 schema 解析、初始 series 事务缺失、效应恢复无法取得 probe 参数、业务 Report 发布通道未闭合，以及 conformance 注册表尚未达到用例级权威标准。

## 2. 任务 A 结果表

| 本方首轮编号 | 结论 | 不接受的理由与定位 |
|---|---|---|
| CS-01 | 不接受 | §0.1 的禁浮点未落实到 `NodeSpec.config`、effect params、Report `ext` 等 digest 输入；§2.3 又使用无序 `frozenset`，与“模型层即为排序列表”矛盾。datetime 的唯一字面形式、NFC 后键碰撞、定点字段小数位也未定义。 |
| CS-02 | 不接受 | §0.2 称 decision/adjudication ID 由编排生成，但 §2.5/§5.1 请求载荷要求调用方提供；token_id 称由 Approval 服务生成，`decision_submit` 却没有 token_id 字段。task_prefix 格式/唯一性、snapshot 截断碰撞处置亦缺失。 |
| CS-03 | 不接受 | §0.3 的版本本身已改为合法的 `1.0.0.dev1`，但推荐锁定表达式 `==1.0.0.dev*` 不是合法 PEP 440 specifier；本地 `packaging.specifiers.SpecifierSet` 会直接报 `InvalidSpecifier`。 |
| CS-04 | 不接受 | V-00/V-04 的引用和路由唯一性已补，但 §1.1 新 schema 无法接受 HLD §4.2.1 的强制旗舰用例：YAML 使用 `from` 而模型仅有无 alias 的 `from_`，单目标 `to` 为标量而模型强制列表。属于修复时引入的 HLD 回归。 |
| CS-05 | 不接受 | V-09 仍把“无显式 join 的多入边”一律拒绝，擅自加严 HLD 的“最大 dispatch 数≤1”。反例：同一节点两个互斥 outcome 都进入 S，S 再进入 join；S 虽有两条入边，但每 activation 最多执行一次，应合法却会被拒绝。 |
| CS-06 | 不接受 | TTL/terminal/指标来源已补，但 `ttl_min` 无正数约束；定点字段未声明小数位；更直接地，§1.1 要求 threshold `value: str`，而 HLD §4.2.1 强制通过的旗舰 YAML 使用数值 `5.0`，且未定义预解析规范化。 |
| CS-07 | 接受关闭 | §2.3 已增加 `produces`、`ProducerSelector`，V-13b/V-15 已给出生产者解析、支配与歧义拒绝规则。 |
| CS-08 | 接受关闭 | C-08a/b 正确区分 paused 与 running；running resolve 保持状态并同事务 durable dispatch。 |
| CS-09 | 接受关闭 | C-13 覆盖 queued/running 预算到期，§3.4 已明确 run/loop/join 计时键、起算点及暂停口径。 |
| CS-10 | 不接受 | publishing 降级已写入，但 `PendingEvent`/`pending_register` 只有 kind 与 dedup_key，无法保存 administrative finalize 的版本漂移证据、series candidate 或 cancel/fail_safe 原因，后续固定映射无法生成规定的 FeedbackReport。 |
| CS-11 | 接受关闭 | §2.2(c) 已封闭定义三种人工裁决、自动 probe、pending 优先级及并发幂等映射。 |
| CS-12 | 不接受 | §0.4 声称“全部完整定义、无匿名 schema”，正文却仍大量使用匿名字典/文字类型体，并缺 SeriesRecord、JoinArrival、完整命令请求/响应等；§1.2、§3.2、§3.6、§4.1/§4.2 还依赖旧 v0.1，v0.2 不能独立作为实现蓝本。 |
| CS-13 | 接受关闭 | `UpstreamItem` 已按 producer+report_type 唯一；所有 Report 统一声明 inputs；BuildReport 已增加 output_refs；ext 容器规则已给出。 |
| CS-14 | 接受关闭 | RunSummary 已移除 corrections；Request/Record 已拆分；ID、operator、时间由服务端生成。 |
| CS-15 | 接受关闭 | squash 已进入 ReleaseManifest；ReleaseTarget/action、目标顺序及重复拒绝规则已定。 |
| CS-16 | 不接受 | C-09 目标列把授权失效分支写成“running 不迁”，但 §2.2(a) 又要求直接按 release_failed 关闭；`decision_submit` 无法提供 token/ci_plan_task，`release_dispatch` 也未传 approved ReleaseManifest 或取得它的响应，审批—核销—执行仍未闭合。 |
| CS-17 | 接受关闭 | §3.1 规则6已明确覆盖规则3，替代 cohort 直接继承被退役者 parent，并逐规则定义 loop_vector。 |
| CS-18 | 接受关闭 | 原“散列 patch 历史而非最终 tree”的核心错误已修正为应用后 Git tree OID；剩余字节编码精确性作为本轮新 issue R2-06。 |
| CS-19 | 不接受 | 四值 ProbeOutcome 与 EffectResult 已补，但“indeterminate 有限重试”没有重试次数、状态持久化或退避来源；probe 能力也无法从当前 `EffectType` 注册面静态判断。 |
| CS-20 | 接受关闭 | `failed_disposition`、`failed_max_retries` 及同键版本化重试/raise 行为已经给出。 |
| CS-21 | 接受关闭 | blob_begin/commit/abort、lease 续期及主要崩溃点/GC 行为已覆盖原问题。 |
| CS-22 | 不接受 | 原语已改为封闭联合，但 `GerritReviewOp.change_id` 可由扩展 adapter 自由提供，且未进入 ReleaseTarget/Manifest 或由 broker 推导；它能改变实际 Gerrit change，却不受审批 digest 绑定。 |
| CS-23 | 不接受 | DataClass/ProviderPolicy 已 schema 化，但 blob_begin/commit 没有 data_class，worker 又可在 HandoffResult 中自报 ArtifactRef.data_class；出生标注与禁止降级没有可强制的数据路径。ToolFact/SourceExcerpt 等成员也缺独立密级来源。 |
| CS-24 | 不接受 | TargetSpec 只有形状，没有 repo/package/manifest 的解析、去重、未知项和 snapshot 选择语义；多数 endpoint 仍用匿名请求/响应；TraceEvent 仍是匿名字典。§0.2 的 `E-SKILL-422-IDEM` 与 §2.7 的 `E-SKILL-IDEM-MISMATCH` 也不一致。 |
| CS-25 | 不接受 | §6 只是复合用例摘要而非逐 case 的输入/动作/期望。`CF-DSL-01..17` 无法一一覆盖 V-00…V-17 加 V-13b，且状态/效应矩阵没有单元格期望；HLD §11.3 多个类别亦无 case ID。 |
| CS-26 | 接受关闭 | C-09 已改为引用 §2.2a，不再错误指向 RestrictedOp 章节。 |

## 3. 任务 B 结果

1. **B-1：反对。**  
   digest 域的类型白名单方向正确，但仍存在 schema 浮点逃逸、无序 frozenset、datetime 唯一字面形式缺失、NFC 键碰撞和定点 scale 缺失。标识符表还与命令载荷的 ID 生成责任冲突，不能保证跨实现一致。

2. **B-2：反对。**  
   C-08a/b、C-11、C-13 与发布 fixed mapping 基本符合 HLD；但 C-09 失效授权分支在表内与 §2.2a 自相矛盾，C-15 的 guard 排除 publishing 却又规定 publishing 时登记 pending，授权载荷链也不闭合。因此状态机尚不能直接实现。

3. **B-3：反对。**  
   §3.1 规则6接受：显式覆盖规则3、父链深度不增长，与 HLD 相容。tree_digest 的“应用后 tree OID”选择也正确，但原始串缺少唯一字节编码、Git OID/应用步骤规范和 framing 约束，尚未闭合到跨实现算法。

4. **B-4：反对。**  
   §6 尚不是 07 可直接执行的权威注册表：没有逐 case fixture/前置状态/期望单元，多个 case ID 实际代表整个矩阵；并遗漏 execution-manifest 恢复、逐 Ledger 边界故障、上游缺失、五类 producer/consumer fixture、attestation 等 HLD §11.3 类别的用例级定义。

5. **B-5：**
   - **规则本身：接受。** ReleasePlan 生产节点支配 release-approval hitl，按 produces 解析并对歧义 fail-closed，能补足审批对象来源约束。
   - **经本轮三方一致批准生效的程序：接受。** 本票同意走该显式勘误通道；三方一致后应先把 V-13b 及勘误记录写回冻结 HLD 基线，再冻结 01，避免形成两个并列权威版本。

## 4. 新 issue 清单表

| 编号 | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|---|---|---|---|
| R2-01 | BLOCKER | §1.1；HLD §4.2.1 | 冻结 HLD 声明旗舰示例必须通过，但当前模型至少三处不能解析该示例：`from_` 无 `from` alias、单目标 `to` 强制为列表、threshold `value` 强制字符串而示例为数值。这是直接 HLD 冲突。 | 定义明确 wire alias；允许 HLD 的 scalar/list 两种 `to` 输入并在模型层规范化为唯一形式；定义 YAML 数值到定点字符串的无损规范化及拒绝边界，并增加旗舰原文直接加载 case。 |
| R2-02 | BLOCKER | §1.3 V-02/V-04、§2.3、§6；HLD §5.1.1/§11.3 | v0.1 原有的“agent_type 已注册、contract 兼容、attestation 与制品/contract/套件版本匹配”校验在重写时消失；V-04 被改作路由唯一性，§6 仅写“照 HLD”。未注册或 attestation 失效的 Agent 无明确拒绝规则。 | 使用空置的 V-02 或新增规则，完整规定注册存在、status_set 非空、contract_requires、task_prefix 唯一、能力及 attestation 绑定校验，并给独立 CF case。 |
| R2-03 | BLOCKER | §2.1 C-04、§2.7 submit_patch、§3.3；HLD §4.2.4/§6.5 | 初始 patch 受理后立即进入 queued 并返回 series_id，但 §3.3 只定义 task_complete 产生的新 series。未定义初始 series 的 parent、current_series_id、lineage、tree_digest 和崩溃幂等事务，首个 TaskInput 因而无确定 series。 | 为 C-04 定义单事务：验证完整 patch series、生成初始 series_id、写 parent/base 与 lineage、计算 tree_digest、推进 current、迁 queued，并规定 request_id 重放和各提交点恢复。 |
| R2-04 | BLOCKER | §2.3 HandoffResult/AgentRegistration、§2.4–§2.5；HLD §6.1/§6.3/§8.2 | `produces` 声明 report_type，但 HandoffResult 只有无约束的 `artifact_refs`，没有 report_type→ref 的规范关系；框架无法可靠构造 UpstreamItem。ReleasePlan 又标称 Report，却未显式继承公共头，缺 task_id/inputs/data_class，授权记录的 ci_plan_task_id 来源不闭合。 | 定义正式 ReportBase 和每个具名 Report 模型；增加独立的 `report_refs` 或规定 artifact_refs 的保留键、唯一性及内容校验；ReleasePlan 必须携带完整公共头并由 gate 实例绑定确切生产 task。 |
| R2-05 | BLOCKER | §4.2 EffectRecord、§4.3 Probe、§5.1 effect_claim；HLD §5.2 | 恢复协议要求 `probe(params, record)`，但 EffectRecord 只持久化 params digest，effect_claim 也只提交 digest。attempt 启动前由编排层对账时拿不到原参数；同时 claim 命令没有 execute/reuse/probe/error 响应联合，也未定义 probe 在哪个进程执行。 | 持久化不可变 canonical params 或受完整性保护的 params artifact ref；定案 probe 执行进程；为 effect_claim 定义包含 idem_key/version/action/stored-result/error 的封闭响应，并覆盖崩溃恢复。 |
| R2-06 | MAJOR | §3.5 | 新算法没有规定 `"\n".join(...)` 的字节编码、是否有末尾换行、tree OID 的对象格式表示、repo_id 字符约束及精确 Git index/apply/write-tree 步骤。不同实现可能得到不同授权绑定 digest。 | 规定长度 framing 或限制并规范化 repo_id；明确 UTF-8、无末尾换行、OID 的 `algorithm:hex` 表示，以及确定性的 read-tree/apply/write-tree 操作与选项；加入跨实现 golden vectors。 |
| R2-07 | MAJOR | §2.1 C-01…C-03、§2.7 POST /runs | POST /runs 成功响应强制含 snapshot_id/ref，但状态机在 create_run 后仍为 created，后续 snapshot_pin 可能失败并 closed(snapshot_error)；协议没有说明请求是否等待 C-02，也没有 snapshot_error HTTP/成功响应形态。 | 明确同步或异步二选一：同步则等待 C-02 并定义失败响应及幂等结果；异步则初始响应只含 run_id/state，由状态查询取得 snapshot。 |
| R2-08 | MAJOR | §2.1 C-15 | C-15 前置 guard 写“created…paused”，排除了 publishing；同一行又规定 publishing 时只登记 pending。不同实现会分别选择拒绝或登记，状态机不再穷尽。 | 拆为 cancel_close（created/awaiting_patch/queued/running/paused）与 cancel_pending（publishing）两个显式分支，分别定义 worker 终止、pending 去重和幂等返回。 |
| R2-09 | MAJOR | §2.4 FeedbackReport | `reports: dict[str, ArtifactRef]` 未定义键；若按 report_type，旗舰图的 review_static 与 review_final 两个 ReviewReport 会发生覆盖，fail_safe 审计证据不完整。 | 改用以 `(producer_node, report_type, task_id)` 唯一的有序项列表，或使用明确的 producer-qualified key，并规定重复与排序。 |
| R2-10 | NIT | 文档头部、A3 | 文档头部与 A3 均称首轮“45 条 issue”，处置表明确为 57 条，影响审计追溯。 | 统一为 57 条；若 45 指去重后数量，应明确写“57 条原始、45 条去重后”。 |

## 5. CLARIFY 清单

无。当前待决项均已达到可定位、可修改的 issue 标准。

## 6. HLD 疑似缺陷登记

无。V-13b 已按本轮指定的 HLD 勘误通道明确投票接受，不另作疑似缺陷登记。

## 7. 一句话总结

距离 draft freeze 仍需先闭合 DSL 与 HLD 旗舰兼容、初始 series、Report 发布、effect probe 恢复和 conformance 用例权威五条主路径，并修正任务 A 中未接受关闭的契约缺口。

上面是Codex的review意见

处置表与 v0.2 全文已核对完毕（含用 grep 对 §6 注册表做的覆盖性实证）。以下为第二轮评审。

---

# 复审报告：01_Contract_Spec v0.2（Kimi 方）

## 1. 总裁决：**NO-GO**

首轮本方 12 条 issue + 2 条 CLARIFY 全部妥善关闭，全量重写质量显著上升。但 §6 用例注册表作为 07 的判定权威，遗漏了 HLD §11.3 的四个测试类别（含 correction、D12 恢复拒绝等发布安全关键项）——01 自 v0.2 起是 conformance 权威来源，该缺口会使 07 harness 与 G6 gate 失去判定依据，按"拿文档直接开发"标准不可放过。1 MAJOR + 3 MINOR/NIT，无 BLOCKER。

## 2. 任务 A：本方首轮 issue 处置核验

| 编号 | 结论 | 说明 |
|------|------|------|
| I-01 (BLOCKER) | 接受关闭 | C-08a/b 拆分→queued；running 不迁移 + 同事务 durable dispatch（CF-SM-02 固化为断言）——HLD §4.4 节点级语义恢复 |
| I-02 | 接受关闭 | "ci 类节点"句已删，V-13b 以 produces 声明 + 支配性形式化（B-5 单独表态见下） |
| I-03 | 接受关闭 | V-00 引用有效性规则完整（route 两端、join.to、LoopEdge 两端须对应已声明 route、metric producer、id/元素唯一），反例形态被拒 |
| I-04 | 接受关闭 | ReleaseManifest 含 squash 且注明"含全部影响远端效果的字段"；簇 CD 仲裁选择（入 manifest 审批面）我接受——HLD §8.2"批准即执行内容"优先于 §6.4 字面归属的论证成立 |
| I-05 | 接受关闭 | §2.3 RunContext 已定义（含 capabilities 实际注入集，"发布通道永不出现"与 D8 呼应） |
| I-06 | 接受关闭 | §0.1 重写：digest 域禁浮点 + 定点字符串 + 类型白名单 + NFC + 字节序 + golden vectors，跨实现一致可执行（残留定点字符串格式见新 M-03） |
| I-07 | 接受关闭 | §5.2 封闭判别联合：语义动词 + content_commit 由 broker 侧构建 + 与 manifest 一一对应校验 + 透传原语明文禁止——比我原建议更严 |
| I-08 | 接受关闭 | §0.2 token_id 单列高熵形态（ulid + 32hex），矛盾消除 |
| I-09 | 接受关闭 | §2.4 patch_digest 计算规则落定（统一 \n 行尾、结尾单换行、不含 description） |
| I-10 | 接受关闭 | §0.2 总表补 gate_instance_id（ulid）与 series_id（SER-+ulid） |
| I-11 | 接受关闭 | §0.2 snapshot_id 显式形式（"SNAP-"+hex 前 32 字符） |
| I-12 | 接受关闭 | §0.4 唯一豁免定案：ext 单一容器 + `<org>__` 前缀，其余 forbid |
| CL-1 | 接受关闭 | §5.1 heartbeat 命令入数据面枚举，双向语义（编排超时=租约失；worker EOF=killpg 自杀）并带 GBS 孙进程注入用例 CF-IPC-05 |
| CL-2 | 接受关闭 | 409/425 拆分 + 全错误码 HTTP 映射入正文，双义消除 |

他方处置抽查：CS-04/V-07 覆盖定义修正（移除声明环边后图无环的等价判定）复核成立；CS-15/簇 CD 的 targets 有序 + 重复拒绝规则合理。未发现错误处置。

## 3. 任务 B：仲裁关注点

**B-1（canonical_json + 标识符总表）：接受。** digest 域禁浮点是唯一能彻底根除跨实现浮点歧义的选择，定点字符串 + 类型白名单 + NFC + 字节序键序 + `-0` 非法构成可判定规则集；golden vectors 提供跨实现裁决基准。标识符总表的生成方/唯一性域/耗尽策略三列使每 ID 可追责。两处残留（定点字符串格式强制、字符串转义约定）见 M-03/M-04，均不破坏闭合性。

**B-2（状态机与发布链重写）：接受。** 逐项比对 HLD：C-08a/b 恢复 HLD §4.4 节点级语义；C-09 三支忠实于 HLD §8.3（失效不核销直接 ledger 级 release_failed 关闭、D12 漂移核销前拦截不烧授权走 C-14）；C-13 预算触顶（含 queued 计入，口径 §3.4 自洽）；pending_register dedup_key 与 §3.3 步 1 的 publishing 降级吻合 HLD"publishing 期仅登记 pending"；§2.2c 裁决映射封闭（confirm_released≡released、confirm_not_released≡release_failed 同 pending 序、keep_unknown 保持、自动 probe 定论等同 broker outcome）+ CAS/幂等，与 HLD §8.3 人工裁决协议逐点对应。未发现偏离。

**B-3（规则 6 显式覆盖 + 新 tree_digest）：接受。** 规则 6 复演：环第 k 轮——规则 4(b) 产 loop activation L（parent=被退役者之父 P），共同 fan-out 重分派时规则 6 覆盖规则 3，子 activation parent 直取 P——祖先链深度逐轮不变，CF-ACT-06 断言该性质；与替代 cohort 继承语义（HLD v0.10）一致。tree_digest 换应用后 git tree OID：bare object DB + 临时 index 无需 worktree 成立（git apply --cached + write-tree 语义），等价 tree ⇒ 等 digest 的性质是内容寻址的直接推论submit 期可应用性校验与完成期计算共用同一算法保证"理论不可达"的防御注释成立；BuildExecutor 复核降为校验的分工正确（定义在规范算法，worktree 仅作存在性对账）。

**B-4（§6 注册表作为 07 权威）：反对（当前形态不充分）。** 以 HLD §11.3 冻结清单逐类对账（grep 实证），四类缺失：①checkpoint/execution manifest 不匹配拒绝恢复（D12 恢复路径全族无 case）；②恢复复用既有 dispatch 记录断言（HLD §4.3.3 的效应键跨重启稳定前提无对应 case）；③post-close correction 三例（迟到检出→correction、重复/并发修正、get_result 呈现——CF-SM-07 只覆盖裁决拒绝，不含 correction）；④五类 agent_type 的 producer/consumer fixture 兼容测试（G6 三条件之一"harness 无判定依据"）。另两类部分覆盖（M-02）。01 既是权威，缺失即 07 不可写、4 gate 不可判。

**B-5（V-13b 勘误）：规则接受，程序接受。** 规则本身：release-approval 型 hitl 要求存在支配它的 ReleasePlan 生产节点，是 HLD §8.2"审批对象 = ci_plan 产出的 ReleasePlan"的静态前提——无支配生产者则审批对象可能不存在，冻结文本确有缺口（HLD-Q1 登记成立）；以 produces 声明判定 + 支配链最近者 + 同深歧义拒绝，可判定且不误伤（旗舰图 ci_plan 支配 human_review 通过）。程序：变更面为新增静态拒绝规则、不触碰任何已冻结的运行时语义，处置表与 01 头部双向显式标注、三方明示批准生效——作为 draft freeze 后的勘误通道可接受；建议（非阻塞）勘误生效后回写 HLD 附录记录保持单一事实源。

## 4. 新 issue 清单

| # | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|------|------|---------|-------------|
| M-01 | MAJOR | §6 | 注册表对 HLD §11.3 冻结清单缺四个类别的用例级定义（grep 实证无对应条目）：①checkpoint/execution manifest 不匹配拒绝恢复（D12 恢复路径）；②恢复重执行复用既有 dispatch 记录（HLD §4.3.3 断言）；③post-close correction（迟到检出→correction 生成、重复/并发修正幂等、get_result 呈现 correction 引用）；④五类 agent_type producer/consumer fixture 兼容（G6 三条件之一）。01 自声明"判定权威在本文档"，缺失即 07 无从实现、P4 gate 无法判定 | §6 补四族 case（如 CF-REC 恢复族、CF-COR 修正族、CF-FIX fixture 族），每族按既有格式给 前置/动作/期望；④至少给出五类 agent 的 fixture 组合矩阵期望 |
| M-02 | MINOR | §6 CF-BLOB / CF-SM | HLD §11.化边界故障注入（**每个** Ledger 提交点前后 kill）"只被 CF-BLOB-02 覆盖 blob 提交点；task_complete/decision_submit/authorization_consume 等复合事务的提交点无参数化注入规则——harness 作者无法判定"每个提交点"的枚举边界 | §6 增一条通则：凡 §5.1/§3.3 标注"单事务"的命令，其提交点前后各注入一次 kill 并断言恢复语义（参数化规则而非逐点枚举即可） |
| M-03 | MINOR | §0.1 规则 2 | 定点字符串"schema 注明小数位"是注释级约定而非强制：生产方写 `"5.0"` vs `"5.00"` 产生不同 digest，跨进程复算分歧（BenchReport.regression_pct、EvidencePacket.confidence 均受影响） | 规定定点字段以 pydantic `Field(pattern=...)` 按声明小数位强制（如 `^-?\d+\.\d{2}$`），或 builder 归一化后落盘；二选一写明 |
| M-04 | NIT | §0.1 规则 3 | 字符串转义约定未写明：非 ASCII 直出还是 `\uXXXX`（大小写）、`/` 是否转义——golden vectors 可兜底但规则层应有句 | 补一句：非 ASCII  UTF-8 直出、仅转义 JSON 必需字符（`"`、`\`、控制字符，`\u` 小写） |
| M-05 | NIT | §0.2 vs HLD §3 | task_id 改 8 位十进制，HLD §3 示例为 `CMP-000123`（6 位）——"形如"非强制故不算冲突，但偏离未注明 | §0.2 该行加注"位数为下限、耗尽扩位（HLD 示例为 6 位形态）"或保持 6 位 |

## 5. CLARIFY 清单

1. §1.5：`benchmark.regression_pct` 解析到 BenchReport 顶层 gateable 字段；若生产节点注册了多个 Report 类型体（produces 多值），`<producer_node>.<field>` 的字段解析目标类型体如何唯一确定（按 field 名唯一匹配？歧义拒绝？）——请确认解析规则。

## 6. HLD 疑似缺陷登记

无新增（HLD-Q1 已经 V-13b 勘误通道处理）。

## 7. 一句话总结

距 freeze 差一处补齐：§6 注册表补四类缺失用例族（M-01，机械可枚举、无设计风险），M-02～M-05 落；本轮其余全部内容（含五处仲裁点）我均已接受，补完即 GO。

上面是Kimi Code的review意见

CodingSystem 01_Contract_Spec v0.2 第二轮评审报告（评审员：Claude Code）
1. 总裁决：NO-GO（无 BLOCKER；3 项 MAJOR——一条校验规则在重写中丢失、gate TTL 无契约表示、conformance 注册表对 HLD §11.3 六类未覆盖；均为定点修复。另 4 MINOR、3 NIT）
总体评价：全量重写质量高，本方首轮 24 条全部按建议或更强方案落地（V-00、C-08a/b、claimed→succeeded、规则 6 显式覆盖、RestrictedOp 封闭联合等），三处结构性变化（tree OID 算法、禁浮点、conformance 权威归 01）方向均正确。NO-GO 的三处都是重写过程的遗漏而非设计问题。

2. 任务 A：本方首轮 issue 处置核验
首轮编号	结论	说明
B-01	接受关闭	V-00 全引用解析 + id/元素唯一，覆盖面完整（含 LoopEdge 须对应已声明 route 的加固）
B-02	接受关闭	"ci 类"原句删除；V-13 回归 HLD 纯字面；替代形态即 V-13b（表态见任务 B-5）
B-03	接受关闭	§4.2 迁移表 claimed→succeeded 合法化并限定对账路径，即本方修法原文
B-04	接受关闭	§3.1 规则 6 改为规则 3 的显式覆盖（parent 直取被退役者之父）+ loop_vector 逐规则复制 + CF-ACT-06 深度不变断言；本方以旗舰两轮迭代复演：F.parent=root、L2.vector 正确递增、深度恒定。附带观察：loop activation 空壳（无 join、无计时器）在 series-非推进环下按轮累积，受 max_rounds 有界，无害
B-05	接受关闭	C-08a/b 拆分，running 不迁移 + 同事务 durable dispatch
M-01…M-05	全部接受关闭	BenchReport 顶层 gateable 字段 + §1.5 唯一数据源；heartbeat 双向语义；V-07 覆盖定义（移除声明环边后无环）；V-09 基数分析（互斥 outcome 合法、多入边拒绝）；RestrictedOp 语义动词封闭联合 + content_commit broker 侧构建 + 一一对应校验
N-01…N-11	全部接受关闭	逐条核对落点属实（禁浮点/token_id 单列/三类型/sys.error 归类/证据 guard/claimed→failed 限定/squash 入 manifest【本方所荐方案】/UpstreamItem/boards/codingsystem.effects/V-08 按成员判定）
T-01…T-03	全部接受关闭	409/425 拆分 + HTTP 映射；序号策略；snapshot_id 形式
C1	部分接受	分工答复成立，但答复所引"V-04 attestation"在 v0.2 已不存在——注册/attestation 校验规则在重写中丢失，见新 issue P-01
C2	接受	§5.1 trace_append 背压约定落文
HLD-Q1	转 V-13b	见任务 B-5
3. 任务 B：五个仲裁关注点
B-1（canonical_json + 标识符总表）：接受。 digest 域禁浮点 + 定点字符串是对跨实现一致性最干净的解法；类型白名单、NFC、UTF-8 字节序键排、golden vectors 使算法可逐字实现；§0.2 把首轮散落的格式矛盾收敛为单一权威表，request_id 幂等域与耗尽策略齐备。注意到 NFC 只作用于 canonical_json 字符串而 patch 内容走独立的原始字节 digest（§2.4 patch_digest），语义敏感字节不被规范化污染——分层正确。

B-2（状态机与发布链重写）：主体接受，附一处 MAJOR 缺口（P-02）。 C-08a/b 与 HLD 节点级 gate 语义逐条吻合；C-09 三支（②不核销直接映射关闭、③专用错误码不烧授权走 C-14）与 HLD §8.3/R3-11 一致；C-13 补齐 queued 期预算；publishing pending 登记与 §3.3 降级、(b) 消费序、(c) 裁决封闭映射（幂等 + CAS + 证据/静默 guard）均忠实。缺口：gate TTL 到期在契约中无处表示——§1.1 有 ttl_min，§2.1 无对应命令行，且 ApprovalDecision.verdict 仅 {approved, rejected}，HLD §4.4 的 outcome=gate_expired 无载体，实现无从表达。

B-3（规则 6 覆盖 + tree_digest 新算法）：接受。 规则 6 的触发条件（分派 task 的 activation 为 loop 型 ∧ 该 fan-out 为其回流的共同 fan-out）可由编译期环表静态判定，父链深度不变经本方复演成立；tree OID 组合获得"等价树同摘要"性质（优于 v0.1 的 patch 序列摘要），apply 失败两级处置齐备，BuildExecutor 复核降为校验的分工成立。一处措辞（"理论不可达"）不准确，见 T-b1。

B-4（conformance 注册表作为 07 判定权威）：反对——尚不充分。 以"写 07 harness"标准核对 HLD §11.3 全类别，注册表缺六类（P-03 列明：随机图 property/fuzz、逐 Ledger 提交点系统性故障注入、恢复期 D12 不匹配→C-14、publishing 期间重启对账优先、post-close correction 三例、五类 producer/consumer fixture 兼容）。在"07 不得改写期望、只实现 harness"的权威关系下，未注册即从 conformance 消失——权威反转的代价是注册表必须完备。已注册的 60+ case 期望均可判定，无不可判定项。

B-5（V-13b 勘误）：规则——接受；程序——接受。 规则：以 produces 声明 + 支配性判定、最近者选取、同深歧义拒绝，正是本方 HLD-Q1 登记时建议的"产物支配规则"形态，不复活"ci 类"概念；对经 join 路径的生产者判定保守拒绝（图论支配不计 all-of 语义），fail-closed 可接受。程序：定点勘误 + 三方在本轮明示表态，实质满足 HLD 冻结条款"任何修改须重走三方评审"的最小充分形式，且通道在首轮处置表已预告、无人异议；建议生效后在 HLD 冻结文档追加 A15 勘误注记，保持单一事实源。

4. 新 issue 清单
编号	等级	定位	问题描述	修改建议
P-01	MAJOR	§1.3（V-02 占位）/CF-DSL	重写丢规则：v0.1 V-04 的"agent_type 已注册 + conformance attestation + contract_requires 兼容"校验在 v0.2 无对应规则（V-04 已改作路由唯一性）；V-05 的"status 全集"解析隐式依赖注册却无显式拒绝路径；CF-DSL-01..17"逐规则一正一反"还包含空占位 V-02，无用例可写	启用 V-02 占位承载该规则（注册存在 + attestation 有效 + contract_requires 与 contracts 包兼容，未注册 agent_type 在 V-05 前拒绝）；同步修正首轮 C1 答复所引编号
P-02	MAJOR	§2.1/§2.5	gate TTL 到期无契约表示：ttl_min 已入 NodeSpec，但 C-表无 TTL 到期命令（C-08a/b 仅消费 ApprovalDecision outbox），且 ApprovalDecision.verdict 枚举无 gate_expired——HLD §4.4 冻结的"超时 outcome=gate_expired 走路由"在本契约中不可实现	增 C-08c：gate_ttl_expire（编排定时）——guard：gate waiting ∧ ttl 到期；gate CAS waiting→resolved(gate_expired)，paused→queued / running 不迁移同事务 dispatch；gate_expired 为编排合成 outcome、不经 ApprovalDecision（verdict 枚举保持人工二值），outbox 项标 origin=ttl
P-03	MAJOR	§6	注册表对 HLD §11.3 六类未覆盖：①随机图 property/fuzz（现仅固定正反例）；②逐 Ledger 提交点系统性 kill 注入（现仅 CF-BLOB-02 覆盖 blob 链）；③恢复期 execution manifest 不匹配 → C-14；④publishing 期间进程重启的对账优先；⑤post-close correction（迟到检出/重复并发修正/结果呈现）；⑥五类 agent_type producer/consumer fixture 兼容	增补 CF-DSL-PROP（随机图性质断言族）、CF-LED（提交点注入族，点位表列举）、CF-SM-08（恢复期 D12→C-14）、CF-SM-09（publishing 重启对账优先）、CF-COR-01..03、CF-FIX-01..05
P-04	MINOR	§4.3	indeterminate"有限重试"无参数落点：failed_max_retries 仅辖 failed 支；indeterminate 的重试次数/间隔无字段无配置键	EffectType 增 probe_retry: {max: int, interval_sec: int} 或声明全局配置键
P-05	MINOR	§5.2 GerritReviewOp	change_id 由业务 adapter 供给，但 content_commit（含 Change-Id trailer 的提交）由 broker 构建——两者一致性规则未述，存在 adapter 供给任意 change_id 与内容不符的缝隙	定案：change_id 由 broker 构建 commit 时生成/校验（op 中该字段改为 broker 回填或删除），与 fencing 同级固定
P-06	MINOR	§2.4 vs §2.5	ReleasePlan 声明为 Report 型（report_type="release_plan"）但 §2.5 模型无公共头字段，且 run_id/series_id 与公共头重复——结构二义	明确 ReleasePlan = 公共头 + §2.5 类型体，去除类型体中与头重复的字段（或声明冗余字段必须与头一致并校验）
P-07	MINOR	§2.3	HandoffResult.artifact_refs 键约定缺失（v0.1"键含所产 Report 类型名"在重写中丢失）——框架按 report_type 提取产物构建 UpstreamItem 依赖此约定	补键约定：所产各 Report 以其 report_type 为键；其余产物键自由但不得与 report_type 冲突
T-b1	NIT	§3.5	"完成事务阶段失败（理论不可达）"不成立：业务 Agent 的 fix patch candidate 未经 submit_patch 预校验，畸形 patch 在完成事务 apply 时可达——防御路径（sys.error）即正路径	措辞改"业务 candidate 首次应用于此，失败 → sys.error"
T-b2	NIT	§4.1	fn: RegisteredEffect 类型名未定义（A0 仅有 EffectType）	统一为 EffectType 注册句柄命名
T-b3	NIT	§1.1	NodeSpec.value 定点字符串无格式校验挂点	V-01 矩阵补 value 的定点正则校验
5. CLARIFY 清单
C-a：digest 域整数是否声明 |n|<2^53 上界（N5 仅 Python 下非必需，但 golden vectors 若供外部工具比对则建议声明）。
6. HLD 疑似缺陷登记
（空——V-13b 已按任务 B-5 走勘误批准。）

7. 一句话总结
距 draft freeze 差三个定点 MAJOR——V-02 恢复注册/attestation 规则、C-08c 补 gate TTL 表示、§6 补录六类 case 族——外加四 MINOR 三 NIT；无架构性问题，修入后本方即转 GO。

上面是Claude Code的review意见
