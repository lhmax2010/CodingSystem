CodingSystem 01_Contract_Spec v0.7 第七轮评审报告（评审员：Claude Code）
1. 总裁决：GO（无 BLOCKER/MAJOR/MINOR；1 NIT 随 GO 列为 freeze 勘误，1 CLARIFY；golden vector 经本方独立复算逐字节吻合）
2. 任务 A：本方第六轮 issue 处置核验
第六轮编号	结论	说明
U-01 (MAJOR)	接受关闭	注册集逐名对齐两份规范原文：本方所列三处缺名（build_failed_exhausted / review_report_ready / release_plan_ready）全部补入，review 集保留 pass 供 A4 terminal；逐 token 复核两份 YAML 的全部 on/success_on（含 gate outcome 的 pass/approved/rejected 不占 agent 集）均落于集内。"harness 从原文字节抽取逐项断言"的自证机制把此类错误从评审面移到红灯面——优于单纯改数据
U-02 (MINOR)	接受关闭	CandidateRecord 完整模型 + 幂等域含 attempt（本方方案一原文落定）：跨 attempt 同 request_id 为新提交、永不取回 discarded 项；attempt 启动事务原子 discard 且不阻塞新登记；CF-SER-10 覆盖
probe 计数语义修正（v0.6 本方接受项被推翻）	接受新方案	Codex 反例成立：v0.6"派发事务计数（保守）"下 spawn 前崩溃循环会不执行任何 probe 而烧光 attempts（假耗尽→假 unknown→假 sys.error）。新方案（派发登记 pending、无 receipt 重派同组不递增、耗尽=attempts==max ∧ 该组 receipt=indeterminate）消除假耗尽且上限仍物理不可破——计数仅随新组递增，crash 循环退化为可运维的活锁而非错误终态。修正成立
3. 任务 B：五个仲裁关注点
B-1（注册集对齐 + 自证机制）：接受。 逐名核对见任务 A；自证机制闭合——CF-FIX-00 的期望不再是静态清单断言而是"从两份原文字节抽取 → 逐项 ∈ 注册集"，注册集与冻结旗舰再失配时 harness 自身红灯，杜绝 v0.6 型回归。

B-2（Envelope 分角色幂等域 vs Skill request_id）：接受。 两层清晰并立：Skill 层 (bearer principal, endpoint) 7 天（HTTP 面）与 control-plane 分角色域（unix socket 面，worker 域含 attempt——与 CandidateRecord 定案同构）互不重叠；receipt 持久化三态（同载荷复用首响应/异载荷 IDEM-MISMATCH/事务后 ACK 丢失安全）覆盖 CF-IPC-06 的三角色×三场景矩阵；§2.2c 裁决幂等随之统一到特权域，AdjudicateRequest 去 id 字段与 §0.2 服务端签发一致。

B-3（CandidateRecord 恢复链）：接受。 时间线走查：attempt1 各崩溃点（submit 前/后、complete 事务前/后）→ attempt2 启动事务原子 discard registered 项且不阻塞、同 request_id 因域含 attempt 取新 candidate_id、分支判定按当前 attempt 活跃数全路径通畅；complete 已提交与 timeout 竞态由单写者串行化天然互斥（consumed 项不在 discard 作用域，无双推进）。恢复不自锁，CF-SER-10 含 kill 点幂等断言可判定。

B-4（receipt 判定序 + 耗尽语义）：接受。 三支以"receipt 存在性 → 组时效"二级键划分，互斥完备（组全序下无第四态）；同组 receipt 优先于过期判定消除了"迟到但有效回报被误拒"；payload digest 校验封住同组异载荷；耗尽判定绑定真实 probe 结果（indeterminate receipt），假耗尽消除、上限不破。对 v0.6 语义的修正成立（任务 A 已述）。

B-5（provider digest golden vector 独立复算）：接受，复算吻合。 本方按 §0.1 规则独立构造 canonical 字节串（键 UTF-8 字节序、无空白、UTF-8）并计算：sha256:fe4ecac852d38fa16109eccad6db71607ae96c8152ed435408beb2ababeca45d——与入档值逐字节一致。封闭文法（装载时校验而非运行时变换）消除实现自由度的思路正确；"模型声明无 secret 字段"比投影规则更强。复算过程暴露一处规则措辞冲突（W7-01）：vector 中 allowed_max:1 按 IntEnum 序列化为整数，而 §0.1 写"Enum 取其字符串值"。

4. 新 issue 清单（随 GO 的 freeze 勘误）
编号	等级	定位	问题描述	修改建议
W7-01	NIT	§0.1 规则 2 vs §5.3/golden vector	"Enum 取其字符串值"与 DataClass（IntEnum）的整数序列化冲突——golden vector 已按整数 1 实算（本方复算确认），规则字面却要求字符串	分型表述："StrEnum 取字符串值；IntEnum 取整数值（DataClass 即例）"
5. CLARIFY 清单
C-vii：endpoint_identity 封闭文法为 https-only，而 FakeLLMProvider 为进程内 fixture 实现（HLD §7.1）——fake/进程内 provider 的 ProviderExecutionConfig 表达约定（endpoint_identity 是否豁免/使用保留标识如 inprocess:fake_llm）建议一句定案，避免 P3 装配时无值可填。
6. HLD 疑似缺陷登记
（空；E-02 候选已在册。）

7. 一句话总结
可以 freeze——第六轮全部 9 条闭合、五个仲裁关注点全过、golden vector 独立复算逐字节吻合，仅余 1 条 Enum 措辞 NIT 与 1 条 fake provider 取值 CLARIFY，随 freeze 编辑落入即可；本方转 GO 并支持在下一轮（若 Kimi/Codex 同轮转 GO）宣布 01 draft freeze。

上面是Claude Code的review意见

## 1. 总裁决：NO-GO

仍有 3 项 BLOCKER：canonical enum 编码矛盾、控制面请求/响应契约未封闭、probe 无回执路径可能无限重派。

## 2. 任务 A 结果表

| 本方第六轮编号 | 结论 | 理由与定位 |
|---|---|---|
| CS-24 | 不接受 | §2.4 的辅助类型已模型化，但 §5.1 仍缺多项 RPC 响应模型，A0 也无法做到其宣称的“类型名↔正文模型一一解析”；见 R7-02。 |
| CS-25 | 接受关闭 | 原问题中的 fixture 状态名已逐项改正。CF-FIX-00 新增的自证算法另有量词错误，作为 R7-05 提出。 |
| R4-01 | 接受关闭 | §3.3 已封闭无 candidate/恰一 candidate 两分支，并以在册数阻断漏填、跨 task 引用和静默丢 patch。 |
| R4-03 | 不接受 | provider_id 排序和 golden vector 已补，但 §5.3 所称 endpoint “封闭文法”仍未封闭；见 R7-04。 |
| R4-04 | 不接受 | 同组 receipt 优先级与 spawn 前恢复已明确，但无 receipt 的持续 worker 崩溃会无限重派；见 R7-03。 |
| R5-02（续项） | 不接受 | §5.1 模型规定 `request_id` 位于 CommandEnvelope，特权运维 prose 却仍写成 `AdjudicateRequest{run_id, request_id,...}`，wire payload 有两种解释。 |
| R6-01 | 不接受 | Envelope 主体已引入，但 request_id 总表、角色作用域、命令识别和响应重放仍未覆盖全部控制面方向；见 R7-02。 |
| R6-02 | 不接受 | 请求模型数量增加，但关键响应联合仍缺失，`CommandEnvelope.payload` 也无法自描述命令类型；见 R7-02。 |
| R6-03 | 接受关闭 | attempt 1 登记后崩溃，attempt 2 原子 discard、同 request_id 在新 attempt 域重新登记、再完成分支 B，路径已闭合。 |
| R6-04 | 接受关闭 | `build_failed_exhausted`、`review_report_ready`、`release_plan_ready` 及 A4 terminal 的 `pass` 均已进入正确 Agent 注册集。 |
| R6-05 | 不接受 | golden vector 正确，但 endpoint 接受域仍存在跨实现自由度；见 R7-04。 |
| R6-06 | 不接受 | 已解决旧 receipt 与 stale 判定冲突，却以无限重派替代了最后一次无回执的终态缺口；见 R7-03。 |

## 3. 任务 B 结果

1. **反对。** 两份 YAML 均可由定案 loader 唯一解析：`on:` 保持字符串键，`join:pre_final_review` 保持完整字符串，`5.0` 规范化为 `"5.00"`，plain `true` 为布尔，标量 `to` 列表化；Agent 来源状态也全部进入正确注册集。但 CF-FIX-00 要求从原文抽取“全部 on/success_on”后断言属于 Agent 注册集，旗舰中的 `approved/rejected` 是 gate outcome，并不属于任何 Agent 注册集，因此自证 case 按字面不能通过。

2. **反对。** Skill 与 control-plane 使用不同 request_id 域在概念上可以并立，同载荷返回首响应、异载荷拒绝及 ACK 丢失重放也合理；但 §0.2 仍把无修饰的 `request_id` 仅定义为 Skill 标识符，§5.1 未给 reconciliation 与编排→broker 方向的域，且缺命令判别和若干响应模型，CF-IPC-06 只覆盖 worker/approval/broker 三类。

3. **接受。** attempt 1 的 registered candidate 在 attempt 2 启动事务中原子转 discarded；attempt 域隔离使同 request_id 可创建新 candidate；当前 attempt 在册数重新成为 1 后，task_complete 可唯一进入分支 B。CF-SER-10 的提交点 kill 期望可判定。

4. **反对。** 三支 receipt 判定本身互斥完备，也消除了 spawn 前崩溃造成的假耗尽；但若 worker 已启动后反复崩溃或超时、始终没有 probe_report，恢复规则会永久重派同一组且不递增。此时永远无法满足 `attempts==max ∧ receipt=indeterminate`，`probe_retry_max` 不再构成实际有限上限。

5. **反对。** 按 golden vector 中 `allowed_max=1` 的整数表示，独立复算输入为：

   `[{\"allowed_max\":1,\"endpoint_identity\":\"https://llm.internal.corp/v1\",\"provider_id\":\"fake_llm\",\"provider_type\":\"openai_compat\",\"routing\":{\"cascade\":[]},\"trust_policy\":{\"ca\":\"corp-root\"}}]`

   结果确为 `sha256:fe4ecac852d38fa16109eccad6db71607ae96c8152ed435408beb2ababeca45d`。但这与 §0.1“Enum 取字符串值”及 §5.3 `DataClass(IntEnum)` 冲突；若把值编码成字符串 `"1"`，结果会变为 `sha256:59a8be0604b058ecb79f06e02ec51f6e8caa45cbb2468964ee24f99ea32ac28e`。endpoint 文法也尚未完全封闭。

## 4. 新 issue 清单

| 编号 | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|---|---|---|---|
| R7-01 | BLOCKER | §0.1；§5.3 ProviderExecutionConfig/golden vector | §0.1 规定“Enum 取字符串值”，但 `allowed_max` 类型为 `DataClass(IntEnum)`，golden vector 又按整数 `1` 计算。两种合法阅读产生不同 digest，破坏 canonical_json 的跨实现唯一性。 | 明确 Enum 使用其 JSON 标量值，并分别规定 StrEnum→string、IntEnum→integer；保留当前 golden vector并增加 DataClass 专属向量。 |
| R7-02 | BLOCKER | §0.2；§5.1；CF-IPC-06；HLD §8.5、附录 B 第5项 | 控制面 wire contract 未封闭：Envelope 无 command/discriminator，所谓“按命令名解析”未说明命令名从何处取得；`authorization_consume` 无响应联合，broker 无法区分“已核销可发布”“授权失效不可发布”“D12 错误”；多个 ACK/错误响应也未定义。request_id 域遗漏 reconciliation 和编排→broker 方向，Adjudicate prose 又与 Envelope 冲突。 | 定义命令名的规范传输位置或封闭请求联合；补 AuthorizationConsume 三支结果及通用 ACK/错误响应；为所有双向角色规定 `(scope, request_id)`，统一 Adjudicate payload，并扩展 CF-IPC-06。 |
| R7-03 | BLOCKER | §4.3 Reconciliation；CF-EF-10；HLD §4.3.3、§5.2 | 反例：最后一组已登记并 spawn，worker 每次都在回报前崩溃。因为始终无 receipt，恢复不断重派同一 probe_attempt 且不递增，既不会耗尽也不会转 unknown/abandoned，administrative finalize 可永久卡住。 | 区分未 spawn 与已开始执行；已开始后无报告的 worker 终止须有限地归并为 indeterminate，或增加独立且有界的无回执重派预算/期限。CF-EF-10 增加连续无报告崩溃直至终态的断言。 |
| R7-04 | MAJOR | §5.3 ProviderExecutionConfig；CF-SM-12 | endpoint 仍不是封闭语法：IPv6 是否必须压缩、DNS 尾点、端口前导零、IPv4 表示及 path 点段均未规定。例如两种小写方括号 IPv6 表示可指向同一端点却产生不同 digest或不同实现接受结果。 | 给出完整 lexical grammar 与 canonicality 规则，覆盖 host、IPv4/IPv6、port 和 path；非唯一表示统一拒绝，并增加边界 negative vectors。 |
| R7-05 | MAJOR | §6 CF-FIX-00、CF-DSL-FLAG；HLD §4.2.1 | “抽取全部 on/success_on 并断言属于注册集”会抽到旗舰 gate 的 `approved/rejected`，而它们正确地不属于 Agent status_set；因此 CF-FIX-00 与 CF-DSL-FLAG 不能按当前期望同时为真。 | 将断言限定为“Agent 来源 route.on + Agent terminal success_on”；gate 来源另对照 §1.2 的固定 outcome 集，保留逐原文字节抽取。 |
| R7-06 | MAJOR | §3.3 CandidateRecord；§5.1 SeriesCandidateSubmitRequest；HLD D6、§4.2.4 | CandidateRecord 只保存 `payload_digest`，通用 receipt 也只保存 digest+响应；后续 task_complete 仅携带 candidate_id，却需要原 candidate_patches 写 lineage、应用 patch 和计算 tree_digest。实现只能依赖未规定的进程内缓存或额外表。 | 在 CandidateRecord 中持久化内容寻址的 patch payload 引用，或明确 receipt 原子保存完整 canonical 请求并由 candidate 引用；补重启/缓存清空后仍能执行分支 B 的用例。 |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

空。E-02 不重复登记。

## 7. 一句话总结

距离 draft freeze 仍需定死 canonical enum 编码、封闭控制面请求/响应、让无回执 probe 有界收敛，并补齐 provider、fixture 自证及 candidate 内容持久化三处契约缝隙。

上面是Codex的review意见

处置表已读、golden vector 已独立复算、注册集与两份 YAML 已逐名核对。以下为第七轮评审。

---

# 复审报告：01_Contract_Spec v0.7（Kimi 方）

## 1. 总裁决：**GO**

本方第六轮唯一 BLOCKER（I-01 注册集矛盾）经簇 HA 妥善修复且引入了防再犯的自证机制；五个仲裁关注点全部接受（B-5 含独立复算通过）。增量审查仅发现 1 条 NIT。01 至此达到我方的 draft freeze 标准。

## 2. 任务 A：本方第六轮 issue 处置核验

| 编号 | 结论 | 说明 |
|------|------|------|
| I-01 (BLOCKER) | 接受关闭 | 簇 HA：CF-FIX-00 注册集逐名对齐——compiler 补 build_failed_exhausted、review 补 review_report_ready（保留 pass 供 A4 terminal）、ci 补 release_plan_ready；旗舰为 HLD 冻结文本不动、只改注册集方向正确。超出原建议的一点值得记录：case 期望改为"harness 从两份原文字节抽取 on/success_on 逐项断言 ∈ 注册集"——注册集与旗舰再脱节即红灯自证，同类错误的复发面被机制性封死 |

## 3. 任务 B：仲裁关注点

**B-1（注册集对齐后双 case 同真）：接受。** 逐名核对：旗舰原文业务 on 值 {build_passed, build_failed_exhausted, ut_passed, ut_fix_patch_ready, bench_done, review_report_ready, release_plan_ready} ∪ gate 值 {pass, approved, rejected} 全部落于 CF-FIX-00 注册集；A4 的 {build_passed, bench_done, pass} 与 success_on=[pass] 同。V-05/V-14 在两文件上可判定且通过，CF-DSL-FLAG 与 CF-FIX-00 同真成立。自证机制（字节抽取断言）闭合该类错误的复发通道。

**B-2（CommandEnvelope 幂等域并立）：接受。** 两层幂等域不重叠：Skill 层 request_id 域为（bearer principal, endpoint)（外部 HTTP），Envelope 层按 socket 角色分域（worker=(task_id, attempt, 命令名）等）——承载通道不同、键空间不同，文本各自标注无歧义。receipt 持久化语义（受理即持久化、同载重放回首响应=ACK 丢失安全、异载 E-CP-IDEM-MISMATCH、保留 Run 存续+7 -IPC-06 三组重放矩阵。一并确认 CandidateRecord 节与 §5.1 对 series_candidate_submit 幂等域的表述一致（均含 attempt）。一处高频命令的界定问题见 M-01（NIT）。

**B-3（CandidateRecord 跨 attempt 恢复链）：接受。** 时间线走查：attempt1 登记（registered）→ 各点崩溃 → attempt2 启动事务**原子** discard 旧项且不阻塞新登记（不自锁）→ 幂等域含 attempt，跨 attempt 同 request_id 视为新 candidate_id（不会取回 discarded 项）→ 当前 attempt 内同载重放同 id、异载拒绝 → 分支 B 走通。"至多一活跃限当前 attempt"的语义边界清晰；task 终局未消费即 discarded 无悬挂。CF-SER-10 期望（旧项 discarded、新登记不受阻、分支 B 走通、kill 点幂等）逐点可判定。

**B-4（probe receipt 判定序）：接受。** 三支互斥完备：有 receipt（校验 payload digest 后返回首响应，优先于过期判定——正确处理"执行完成后才过期"的时序）／无 receipt 且有更高组（stale 拒并审计）／无 receipt 且当前组（受理并持久化）；三态按（receipt 存在性 × 组新旧）穷尽。修正成立：v0.6"spawn 前崩溃已计数"的保守语义确会造成假耗尽（计数了从未执行的尝试改为"计数=新派发事务、pending 无 receipt 重派同组不递增"后，耗尽判定（attempts==max ∧ 该组 receipt=indeterminate）只统计真实执行过的尝试，假耗尽消除，上限仍不可突破（递增只在派发事务、重派幂等）。probe 只读无副作用使重派重执行安全。CF-EF-10 逐点断言覆盖。

**B-5（provider golden vector）：接受，独立复算通过。** 按 §0.1 规则自行实现 canonical 编码（键按 UTF-8 空白、整数无冗余）对给定输入复算，并直接哈希文档所印字面，两者均得 `sha256:fe4ecac852d38fa16109eccad6db71607ae96c8152ed435408beb2ababeca45d`，与文档所录完全一致。封闭文法（provider_id ASCII、endpoint 装载时校验即规范化、无 secret 字段）消除实现自由度；golden vector 实算入档且可复算，CF-SM-12 判定锚成立。

## 4. 新 issue 清单

| # | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|------|------|---------|-------------|
| M-01 | NIT | §5.1 通用段 | "全部 control-plane 命令经 CommandEnvelope 提交 + 受理即持久化 receipt"字面覆盖 heartbeat——每秒级高频命令逐条持久化 receipt 且保留 Run 存续+7 天，receipt 表无谓膨胀；心跳的幂等语义本就平凡（seq 单调），receipt 无收益 | 明示 heartbeat（与可选的 trace_append）免于 receipt 持久化，或为其规定仅记最新 seq 的特化规则 |

## 5. CLARIFY 清单## 6. HLD 疑似缺陷登记

无新增。

## 7. 一句话总结

可以 freeze——01 七轮评审后注册集、命令面、幂等域、candidate/probe 恢复链、provider digest 全部闭合并经实证（含 golden vector 独立复算一致），建议将 M-01 作为 freeze 勘误随 v0.7 定稿。

上面是Kimi Code的review意见
