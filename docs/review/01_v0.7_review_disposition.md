# 01_Contract_Spec v0.7 三方复审处置表（→ v0.8）

裁决输入：Kimi **GO**（1 NIT）、Claude Code **GO**（1 NIT + 1 CLARIFY，golden vector 独立复算逐字节吻合）、Codex NO-GO（维持 5 + R7-01..06：3 BLOCKER + 3 MAJOR）。合计 9 个独立问题，**全部采纳**。
**实证里程碑**：golden vector 三方独立复算全部一致（Kimi 双路径复算、CC 独立构造、Codex 复算并给出另一读法的反例 digest）——canonical 规则首次经受跨实现检验，同时精确暴露 R7-01。

## 交叉簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| IA Enum 编码两读 | CC W7-01(NIT); Codex R7-01(BLOCKER)——同发现异评级，Codex 连异读 digest（sha256:59a8…）都实算出来 | §0.1 分型定案：StrEnum→字符串、IntEnum→整数（DataClass 即例）；既有 vector 即 IntEnum 专属向量 |
| IB gate outcome 误入自证断言 | Codex R7-05(MAJOR)；**Kimi B-1 复核结论与之相反（把 approved/rejected 归入注册集覆盖）——Codex 对，Kimi 误** | 断言量词修正：仅 agent 型来源 route.on + agent terminal success_on 对注册集；gate 来源对照 §1.2 固定集。两家结论冲突的裁决依据：§2.3 status_set 定义为 HandoffResult.status 词表，gate outcome 由框架合成、不经 HandoffResult |

## Codex——全采纳
R7-02 BLOCKER → Envelope 补 command 判别 + **封闭命令注册表**（命令名↔请求↔响应三列）+ AuthorizationConsumeResponse 三支判别联合（granted/rejected(reason)/d12_mismatch——broker 决策 wire 化）+ CommandAck/CommandError 封闭码 + reconciliation 与编排→broker 出向幂等域补齐 + Adjudicate prose 与模型统一（R5-02 续项同解）+ CF-IPC-06 扩五方向。R7-03 BLOCKER → 已 spawn 无回执定案：worker 终止无 receipt → 编排合成 indeterminate receipt（origin=worker_loss）消耗该次尝试——连续崩溃有界收敛到 unknown+sys.error，"未 spawn 重派不递增"与"已 spawn 合成消耗"两支封闭（其无限重派反例成立：v0.7 修 spawn 前崩溃时把另一头敞开了，如实记录）。R7-04 → endpoint 完整词法文法（dns 无尾点/IPv4 无前导零/RFC 5952 压缩 IPv6/端口规则/禁点段）+ negative vectors 交付约定。R7-06 → CandidateRecord.payload_ref（受理事务持久化 canonical 全文为内容寻址 artifact，分支 B 不依赖进程内缓存）+ CF-SER-11 重启用例。
维持项 CS-24/R6-01/R6-02 → 随 R7-02 命令注册表与响应联合终解；R4-03/R6-05 → 随 R7-04；R4-04/R6-06 → 随 R7-03。

## Kimi / Claude Code——全采纳
Kimi M-01 → heartbeat 免 receipt（仅记最新 seq）、trace_append 以 (task, attempt, 首 seq) 天然去重豁免。CC W7-01 → 簇 IA；C-vii → endpoint 文法增 `inprocess:<provider_id>` scheme（进程内 fixture 专用）——FakeLLMProvider 装配取值定案。

## 汇总
- 9 条全采纳。裁决记录一条：簇 IB 中两家 GO 方与 NO-GO 方对同一断言得出相反结论，按 status_set 定义裁 Codex 对——异构评审的分歧本身暴露了文本歧义，量词修正后消除。
- v0.8 复审重点：(1) 命令注册表与响应联合的封闭完备（以"写 broker/worker 客户端"标准）；(2) 已 spawn 合成 receipt 的有界收敛与未 spawn 重派的两支互斥；(3) endpoint 文法 negative vectors 可判定；(4) payload_ref 链路（CF-SER-11）；(5) 簇 IB 量词修正后自证断言在两份 YAML 上的复演。
