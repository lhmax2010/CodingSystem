# 01_Contract_Spec v0.1 三方评审处置表（→ v0.2）

裁决输入：Kimi NO-GO(1 BLOCKER+6 MAJOR+5 MINOR/NIT)、Claude Code NO-GO(5 BLOCKER+5 MAJOR+11 MINOR+3 NIT)、Codex NO-GO(24 BLOCKER+1 MAJOR+1 NIT)。共 57 条（重叠合并后约 40 个独立问题）。**全部采纳**（两处取等效方案，注明）。v0.2 为全量重写。

## 共识簇（三家或两家同发现）

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| CA C-08 节点级 gate 语义走样（三家 BLOCKER 级同打） | Kimi I-01; CC B-05; Codex CS-08 | §2.1 拆 C-08a/b：paused→queued；running 不迁移、同事务 durable dispatch |
| CB V-13 "ci 类节点"复活已废概念 + HLD-Q1 | Kimi I-02; CC B-02+HLD-Q1; Codex CS-07 | 原句删除；新 V-13b 以 produces 声明（"release_plan"）+ 支配性形式化，标注 HLD 勘误 HLD-Q1，随本轮复审批准生效；AgentRegistration 增 produces（CS-07 一并解） |
| CC′ 引用有效性漏规则 | Kimi I-03; CC B-01; Codex CS-04 | 新 V-00（全引用解析 + id 唯一 + 元素唯一）+ V-04（(from,on) 唯一、fan-out 单条多元素 to）+ V-07 覆盖定义修正（CC M-03） |
| CD squash 审批断裂 | Kimi I-04; CC N-07; Codex CS-15 | 取"纳入 ReleaseManifest 进审批面"方案（三家备选之共项；HLD §8.2"批准即执行内容"优先于 §6.4 字面归属，处置表明示此澄清）；ReleaseTarget/action 封闭模型 + targets 有序去重规则 |
| CE canonical_json 浮点不可执行 | Kimi I-06; CC N-01; Codex CS-01 | §0.1 重写：digest 域禁浮点（定点字符串）、类型白名单、NFC、字节序、前缀形式、golden vectors |
| CF′ token_id 格式矛盾 + 标识符缺表 | Kimi I-08/I-10; CC N-02/T-02; Codex CS-02 | §0.2 标识符总表（格式/生成方/唯一性域/耗尽策略），token_id 采高熵形态单列 |
| CG activation 规则 3+4b 组合父链增长 | CC B-04; Codex CS-17 | §3.1 规则 6 改为规则 3 的显式覆盖（parent 直取被退役者之父）；补 loop_vector 逐规则复制；CF-ACT-06 深度不变断言 |
| CH tree_digest 算历史非终树 | Codex CS-18（Kimi I-09 的 patch_digest 未定义为其子集） | §3.5 重写：bare object DB 应用后取 git tree OID 规范组合；等价 tree 用例；BuildExecutor 复核降为校验；patch_digest 计算规则补于 §2.4（Kimi I-09） |
| CI′ RestrictedOp gerrit_rest 过宽 | Kimi I-07; CC M-05; Codex CS-22 | §5.2 改封闭判别联合（git_push/gerrit_review_push，语义动词 + content_commit 由 broker 侧构建 + 与 manifest 一一对应校验 + FencingInfo 定型）；透传原语禁止 |
| CJ upstream 键冲突 + Report 缺失标注不统一 | CC N-08; Codex CS-13 | §2.3 UpstreamItem(producer_node+report_type)；公共头统一 inputs；BuildReport 补 output_refs；ext 容器 + 前缀约定（Kimi I-12 同解） |
| CK RunContext 等模型缺席 | Kimi I-05; Codex CS-12 | §2.3/§2.5/§2.6/§4.2/§5.4 补全：RunContext/ApprovalDecision/ExecutionManifest/ReleaseTarget/EffectRecord/预算族/RoundDigest/PendingEvent/CorrectionRequest·Record/SysOutcome/DataClass/EvidencePacket 族/TraceEvent；A0 类型索引对账 |
| CL′ heartbeat 通道缺席 | Kimi CL-1; CC M-02 | §5.1 heartbeat 命令 + 双向语义定案（编排超时=租约失；worker EOF=通道失自杀） |
| CM E-SKILL-409 双义 | Kimi CL-2; CC T-01 | 拆 409/425 + 全错误码 HTTP 映射入正文（Codex CS-24 同解） |

## Codex 独有条目（其余全采纳，逐条落点）
CS-03 版本号 PEP 440 → §0.3 改 1.0.0.dev1 + 映射与锁定策略。CS-05 V-09 基数分析（与 CC M-04 合并）→ V-09 重写（互斥 outcome 合法、多入边拒绝）。CS-06 gate TTL 字段/metric 数据源统一/terminal 无出边/字段适用矩阵 → §1.1/§1.5/V-14（与 CC M-01 BenchReport 顶层 regression_pct 合并）。CS-09 queued 预算/计时键 → C-13 + §3.4。CS-10 publishing pending 命令与 §3.3 guard → §2.1/§3.3。CS-11 裁决封闭映射 → §2.2c。CS-14 corrections 双源/自报字段 → RunSummary 移除、Request/Record 拆分、服务端生成。CS-16 授权三支/新鲜度 CAS 载荷/token 选择 → §2.2a/§2.5/§5.1。CS-19 ProbeResult 四值 + EffectResult envelope → §4.3（indeterminate 有限重试后 unknown，不当 not_happened）。CS-20 failed disposition → EffectType 两字段 + §4.3。CS-21 blob lease 协议 → blob_begin/commit/abort 三命令 + 崩溃点表。CS-23 DataClass schema 化/缺失按 secret → §5.3 + ArtifactRef.data_class。CS-24 TargetSpec 判别联合/request_id 作用域/trace envelope → §2.7/§0.2/A1。CS-25 conformance 权威归 01 → §6 用例注册表（case ID + 期望，07 只做 harness——其批评成立，权威关系反转已纠正）。CS-26 交叉引用 → 修正。

## Kimi / Claude Code 其余条目（全采纳）
Kimi I-11 snapshot_id 显式形式（§0.2）；I-12 ext 约定（§0.4）。CC B-03 claimed→succeeded 对账迁移合法化（§4.2，其修法原文）；M-03 V-07 覆盖定义；M-04 并入 V-09；N-03 三类型补齐；N-04 series CAS 归 sys.error（其论证成立，invalid_result 语义不符）；N-05 裁决证据 guard（§5.1）；N-06 claimed→failed 限定；N-09 UTReport boards 列表 + suite 级 board；N-10 EffectType 注册面（codingsystem.effects）；N-11 fan-out to 含 join 成员 → V-08 按成员判定（允许 fan-out 直入 join 需同划分规则约束，V-10 覆盖）；T-02 序号策略；T-03 语序修正。CC C1 → V-04 attestation 与 D12 分工：加载校验 attestation、dispatch 校验 manifest，交叉项 = agent_artifacts 含该制品 digest（§2.6 复核时点已列）；C2 → trace_append 背压约定入 §5.1。

## 汇总
- 57 条全采纳；两处方案择定：CD 取"入 manifest"（对 HLD §6.4 字面的澄清，处置表明示）；CS-19 的 indeterminate 处置取"有限重试后 unknown"（较其"归类重试"更具体）。
- v0.2 复审重点：(1) §0.1 canonical 规则与 §0.2 标识符表是否闭合；(2) §2.1/§2.2 状态机-发布链重写后与 HLD 的逐条一致性；(3) §3.1 规则 6 覆盖与 §3.5 新算法；(4) §6 用例注册表作为 07 权威是否充分；(5) V-13b 勘误的三方批准（明示表态项）。
