# 01_Contract_Spec v0.10 三方复审处置表（→ v0.11）

裁决输入：Kimi **GO**（连续第二轮零 issue）、Claude Code **GO**（连续第四轮，1 MINOR + 1 NIT；对 X8-01 误判如实认领）、Codex NO-GO（维持 4 组 + R10-01..08：3 BLOCKER + 4 MAJOR + 1 NIT）。合计 10 个独立问题，**全部采纳**。
**B-3 表态定案**：入向"幂等域存续期全量 digest 对 + Ack 重建"三方一致认定为零豁免终案、无需勘误——语义争议结束；Codex 对出向的保留为模型缺席（R10-03），本版落模型后不再列表态项。

## 交叉簇

| 簇 | 覆盖 | 处置落点 |
|----|------|---------|
| KA release_outcome(indeterminate) 与冻结两值集冲突 | CC Z10-02(NIT，从 C-10 表格侧发现)；Codex R10-01(BLOCKER，指出修法方向须反向) | **取 Codex 方向**：release_outcome 回归 {released, release_failed} 两值、仅执行后发送；恢复臂对账改经新命令 `release_reconcile_report`（非 release node outcome，不触 C-10）——happened → ledger 级 released 映射（HLD §4.3 字面）、not_happened → 原子许可、indeterminate → 记录保持 publishing 走 C-11；新增 C-10a 行。v0.10 给 outcome 集加值属对 HLD §4.2.5 的违反，如实记录 |
| KB broker_epoch 无约束/无获取通道 | CC Z10-01(MINOR)；Codex R10-04(MAJOR) | 新命令 `broker_hello`（建连握手，编排按启动单调分配、绑定连接上下文、范围 1..2^53）；authorization_consume 的 broker_epoch 与连接 epoch 等值校验（不等/未握手 → E-CP-FORBIDDEN，CF-AUTH-12）；broker socket 签名补正 |

## Codex——全采纳
R10-02 BLOCKER → not_happened 后**编排同事务原子许可三件套**（manifest 匹配 ∧ epoch 现行 ∧ 无 pending admin_finalize ∧ 授权仍 consumed_by 本 task）→ proceed / deny(reason)；deny 时 broker 不发布、发 release_outcome(release_failed, proceed_denied)（远端已证未发生，两值映射合法）→ §2.2b admin_finalize 优先消费；CF-AUTH-05b 竞态注入。R10-03 BLOCKER → DispatchReceipt 具名模型（Ledger 表，唯一键 (token_id, request_id)，state intent/completed，首响应）+ 两事务出向协议（发送前 intent / ACK 后 completed）+ 恢复重派与同 id 异 digest 拒 + CF-IPC-10 四崩溃点——出向零豁免至此有模型承载。R10-05 → CommandResponse 第三臂 FramingError（关联字段可空、raw_command 回显）+ CF-IPC-09 三形态。R10-06 → CF-AUTH-04/CF-SM-12 限定 running，各增 publishing 变体（pending 恰一次、不关闭）。R10-07 → 处理序定案"先 receipt 后 guard"，already_consumed 限不同 request_id，CF-AUTH-11（ACK 丢失同 id 重投仍 granted）。R10-08 → 头部轮数更正。
维持项 CS-24/R6-02/R7-02、R6-01、R8-01、R8-03 → 随 R10-01/02/03/05 终解。

## Claude Code——全采纳
Z10-01 → 簇 KB；Z10-02 → 簇 KA（其"补第三出口"建议方向被 Codex 依 HLD 字面反转，C-10 保持仅 closed，本表说明）。

## Kimi——GO，零 issue（连续第二轮）

## 汇总
- 10 条全采纳；一处对 HLD 冻结集的违反如实记录（indeterminate 入 outcome 集），一处 CC 建议方向被 HLD 字面反转（Z10-02）。CommandName 扩至 19。
- v0.11 复审重点：(1) 恢复臂全链（resumed → reconcile_report 三结果 → 原子许可/ledger 映射/记录）与两值 outcome 集在四条时间线上的闭合；(2) DispatchReceipt 两事务协议四崩溃点；(3) broker_hello 握手与 epoch 等值校验；(4) FramingError 臂与既有 ResponseError 的边界；(5) 三处新命令/模型在注册表、A0、§5.1 文字面的一致性。
