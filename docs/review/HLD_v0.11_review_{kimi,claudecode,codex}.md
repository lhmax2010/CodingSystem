## 1. 总裁决：GO

无 BLOCKER、MAJOR 或 MINOR；1 项 NIT 可作为 freeze 勘误处理。

## 2. 任务 A 结果表

| 本方第十轮编号 | 接受关闭/不接受 | 不接受的理由与定位 |
|---|---|---|
| R10-01 BLOCKER | 接受关闭 | §4.2.6 已增加“重经自身共同 fan-out 前不得先重经任何祖先共同 fan-out”的静态约束，第十轮 `F0/F1` 反例被拒绝；§11.3 也增加了对应 validator property。 |
| R10-02 MAJOR | 接受关闭 | §5.2 已枚举 `succeeded/abandoned` 放行异参新键、旧 `succeeded` 不跨参数复用、`unknown` 阻断新键，并由 §11.3 动作矩阵固化。 |
| R10-03 NIT | 接受关闭 | §11.3 已将粘连的引号和用例重排为四组明确的 model-based 断言，原格式问题消除。 |

## 3. 任务 B 结果

1. **条件(iii)第二条静态约束：接受。**

   多层嵌套边界闭合：

   - 第十轮反例中，Y 属于 F1 产生的内层 activation，环路在重经 F1 前先重经祖先 F0，因此被明确拒绝。
   - 旗舰图中，UT 回到 compiler 后首先重经产生当前 cohort 的 compiler fan-out，且不存在先行的祖先 fan-out，因此继续合法。
   - 内层作用域环首先返回自身 F1，退役内层 activation、替代 cohort 继承原父 scope，外层 activation 不受影响。
   - 内层 join 完成后，下游已经恢复到父 activation scope；此时返回父 fan-out 属于返回“自身”fan-out，不会被误拒。
   - 对任意深度 Ak，规则要求先重经 Fk，再允许涉及祖先 Fk−1…F0，正好排除了运行时需要退役祖先、但条件③只锚定 Ak 的图形。

   该约束只裁掉当前运行时模型无法正确执行的跨作用域环，不误伤已支持的旗舰图、同层环或 join 后父作用域环。

2. **异参新键允许集合：接受。**

   与既有机制相容：

   - 旧 P1 定态为 `succeeded`，新 P2 因参数摘要不同使用独立键；P1 只接受 P1 的精确键复用，不会被错误返回给 P2。
   - 旧 P1 为 `abandoned`：同参重做仍按 §5.2 版本化通则建立同键新版本；异参 P2 是独立新键，不需要套用同键版本化。
   - 旧 P1 为 `unknown`：attempt 已以 `sys.error` 中止，不存在新 claim 窗口。
   - 同 attempt 内 P1 仍处于 `claimed/running` 时提交异参 P2，仍按原规则拒绝并返回 `sys.invalid_result`；它与“attempt 启动前已完成定态”的跨 attempt 路径不重叠。

## 4. 新 issue 清单表

| 编号 | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|---|---|---|---|
| R11-01 | NIT | §4.2.6 | 原文“违反任一约束的回环拒绝加载，**否则拒绝加载**”存在指代粘连；按字面会变成违反与不违反约束都拒绝，与旗舰示例必须通过及 §11.3 只拒绝两类非法环不符。上下文足以确定设计意图，故不阻塞 freeze。 | 分离两个拒绝条件，例如：“违反任一环约束的回环拒绝加载；未满足上述三类之一的其他 outcome 分支亦拒绝加载。” |

## 5. CLARIFY 清单

无。

## 6. 一句话总结

可以 freeze；R11-01 可作为 freeze 勘误同步修正。

上面是Codex的review意见

两份材料已读完，并用 diff 确认 v0.11 相对 v0.10 的修改确实仅三处（§4.2.6 条件（iii)、§5.2 允许集合枚举、§11.3 格式与用例）加头尾版本行。本方第十轮为零 issue GO，无待核验条目。以下为第十一轮评审。

---

# 复审报告：00_Framework_HLD_v0.11（Kimi 方）

## 1. 总裁决：**GO**

第十轮本方零 issue，无处置核验负担；两个仲裁关注点复演后均接受。新增审查发现 1 条 MINOR（允许集合枚举漏列 failed 态）+ 1 条 NIT（条件（iii）重复"拒绝加载"措辞），作 freeze 勘误。

## 2. 任务 A：本方第十轮 issue 处置核验

| 编号 | 结论 | 说明 |
|------|------|------|
| —（ issue） | 不适用 | 处置表如实记录"Kimi——GO，零 issue"。本轮附带复核：v0.11 diff 确认修改范围与任务书声明一致（仅三处句级修改），我第十轮"既关闭条目回归抽查通过"的结论不被新文本破坏（§4.2.4 条件③三件套原文未动、§5.2 其余段落逐字保留） |

## 3. 任务 B：仲裁关注点独立判断

**B-1（条件（iii）第二条静态约束）：接受。** 演第十轮反例图 `F0→[L, F1→(X,Y)→J1]→J0`、Y 环边越过 F0：该边对 J0（共同 fan-out=F0）与 J1（共同 fan-out=F1各自满足第一约束（两 fan-out 均在回环路径上），旧规则放行；但采用边的 Y task 属内层 activation I，③退役根锚在 I，真正被替代的外层作用域 O 不被标记——O 的计时器存活、祖先链 CAS 不设防，与 Codex 分析一致。第二约束"重经自身 activation 的共同 fan-out 之前不得先重经任何祖先 activation 的共同 fan-out"按回环路径上 fan-out 的遭遇次序判定：该图回环先遇 F0（祖先）→ 拒绝加载 ✓。不误伤核验：①旗舰图——ut_fix 边 ut→compiler，ut 所属 activation 的共同 fan-out 即 compiler 自身 fan-out路径上无祖先 fan-out（根 activation 无 fan-out），约束二空虚满足 ✓；②内层作用域环（回环先遇 F1 不越界）→ 放行，且退役根=被替代作用域 ✓；③组合场景（回环先经自身 F1、后段再越 F0）由路径后段的另一条环边在其自身上下文中被约束二拦截，约束可组合 ✓。退役根=被替代作用域的静态保证成立。

**B-（异参新键允许集合枚举）：接受，附一条完备性勘误（M-01，MINOR）。** 相容性逐项核对：succeeded 不跨参复用与查找矩阵"succeeded→直接复用"以"同参精确键命中"为界划清——矩阵管同参命中、管异参新键，两者不重叠；abandoned 放行新键与记录版本化通则（同键重执行版本化承载）方向一致（异参即新键，天然独立记录）；unknown 无新键与"unknown→sys.error 中止 task"闭环。枚举使"允许继续的终态"从模糊措辞变为可判定集合。缺口：枚举列了 succeeded/abandoned/unknown，六态中的 **failed** 未列——同 task 内异参新 claim 遭遇 failed 终态记录时放行还是拒绝未定义（failed 的既定处置是"按效应类别策略重试或上报"，但若效应类别策略为上报后 agent 继续——同 attempt 异参调用的作者误用形态与 claimed/running 情形同构，现行拒绝规则却只覆盖未决记录）。一句补丁即可（建议 failed 比照未决记录拒绝或显式放行）。

## 4. 新 issue 清单

| # | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|------|------|---------|-------------|
| M-01 | MINOR | §5.2 | 异参新键允许集合枚举不完备：枚举定案列出 succeeded/abandoned（放行）与 unknown（无新键），六态中的 failed 未列——同 task 遭遇 failed 终态记录的异参新 claim 行为未定义。反例形态：attempt 内 build_main(A)、类别策略上报后 agent 继续执行并对同 call_id 以异参 B 再 claim（与 claimed/running 情形的作者误用同构，但运行时拒绝规则只覆盖"未决"记录，failed 是已决记录） | 枚举补 failed 态归属：建议比照误用拒绝（sys.invalid_result），或显式声明放行新键；§11.3 定态×异参动作矩阵补对应行| M-02 | NIT | §4.2.6 | 条件（iii）末段"违反任一约束的回环拒绝加载，否则拒绝加载（互斥 outcome 分支……）"——"拒绝加载"仍出现两次，"否则"的挂靠（三分支均不满足的情形）需读者回跳段首重建；第十轮 AB- 的"仅一次收尾"意图未完全落实 | 重排为"……违反任一约束的回环拒绝加载；不满足三者之一且不构成合法环边例外的分支，拒绝加载（互斥 outcome……）"或直接拆成两个编号子句 |

## 5. CLARIFY 清单

空。

## 6. 一句话总结

可以 freeze——v0.11 三处句级修改经复演与 diff 全量核对均闭合，建议将-01（failed 态归属一句）与 M-02（措辞）作为 freeze 勘误随 v0.11 定稿后进入 01_Contract_Spec 编写。

上面是Kimi Code的review意见

CodingSystem Framework HLD v0.11 第十一轮复审报告（评审员：Claude Code）
1. 总裁决：GO（无 BLOCKER/MAJOR/MINOR；1 条范围词 NIT 随 GO 列为 freeze 勘误）
三处句级修改核验闭合。R10-01 的组合反例我已复演确认成立——如实说明：本方第十轮 B1 复演所用图形态（环回自身作用域 fan-out、嵌套内层环）未覆盖跨 activation 边界的回环，Codex 的构造暴露了该盲区；本轮我以多层嵌套图对第二约束做了针对性复演，确认修复正确且不误伤。

2. 任务 A：本方第十轮 issue 处置核验
第十轮编号	结论	说明
AB-01 (NIT)	接受关闭	§4.2.6 条件(iii)已按"合法条件——两条静态约束（机制括注附于约束一之后）——拒绝情形"重排，随 R10-01 第二约束落文一体完成；原括注错位消除。句中仍有两处"拒绝加载"，但指涉已清晰区分（违反环边约束的回环 vs 三分支皆不满足的 outcome），不再构成误读面，不另立 issue
3. 任务 B：两个仲裁关注点
B1（条件(iii)第二条静态约束）：接受。 多层嵌套复演：

第十轮反例图的拒绝：F0→[L, F1→(X,Y)→J1]→J0 中 Y 的环边越至 F0——Y 的 task 属内层 activation I（own fan-out = F1），回流路径先经祖先 O 的 fan-out F0 才及 F1，违反约束二 → 拒绝加载 ✓。该图在 v0.10 下确实对 J1、J0 各自满足约束一而被放行，随后退役根错锚于 I、真正被替代的 O 逃过退役（计时器存活、祖先链 CAS 不设防、父继承多嵌一层）——反例成立，修复必要。
旗舰图保留：ut→compiler，ut 属 A1（own fan-out = compiler），直接重经自身 fan-out；根 activation 无关联 fan-out，约束二空洞满足 ✓。
内层作用域环保留：内层环回自身 F2，不经外层 F1 ✓。
三层嵌套一致性：O⊃I1⊃I2 下，I2 内 task 回 I1 的 fan-out → 拒绝（退役根会错锚 I2）✓；回自身 F2 → 合法，级联退役 I2 子树 ✓——判定在任意深度与"退役根 = 被替代作用域"不变量一致。
表达力：整层重做仍可表达——环边须从该层作用域内的 task 发起（如内层 join 结果路由至外层节点后再回流 F0，此时 task 属 O、own fan-out = F0，合法），即"想替代谁，就从谁的作用域回流"，与运行时锚定语义同构。静态判定所需的祖先 fan-out 关系可从嵌套结构静态导出，"必然重经"的全路径语义沿约束一惯例，[01] 可形式化。
B2（异参新键允许集合枚举）：接受，附 1 条范围词 NIT。 与既有规则逐条比对无矛盾：succeeded 放行异参新键且"仅同参精确键复用、不得跨参复用"——与查找矩阵按键命中一致（该句的价值是封死按 call_id 粗粒度复用的实现捷径）；abandoned 放行 + 同键场景由记录版本化通则承载 ✓；unknown 随 sys.error 中止、无新键可立，与 fail-closed 一致 ✓；与"仅同一 attempt 内未决记录异参 claim 拒绝"规则正交（枚举辖终态、拒绝辖未决）✓。唯一措辞面：枚举以"定态"开头，意在只辖跨 attempt 启动对账的三支产物，但"允许集合（枚举定案）"的标题若被读作全局门禁，会错误封锁"干净终态 failed 之上同 call_id 异参自适应重试"（failed 不在枚举中，而它既非未决也非定态产物，按既有类别策略应放行）——见 AC-01。

4. 新 issue 清单（随 GO 的 freeze 勘误）
编号	等级	定位	问题描述	修改建议
AC-01	NIT	§5.2	"异参新键允许集合（枚举定案）"三分支均以"定态"限定，意为仅辖跨 attempt 启动对账产物（三支恰为 succeeded/abandoned/unknown），但若被读作异参新键的全局允许集，则干净终态 failed（正常记录、非定态产物）之上的同 call_id 异参新 claim 会被误判为不允许——与"failed → 按效应类别策略重试或上报"及运行时拒绝仅辖未决记录的既有规则不符	枚举句补一短语："（本枚举仅辖跨 attempt 定态记录；干净终态 failed 之上的异参新键依既有类别策略，不在此列）"
5. CLARIFY 清单
（空）

6. 一句话总结
可以 freeze——第二静态约束与异参枚举均经复演闭合，十一轮累计的全部架构主题（DSL 静态检查体系、审批-发布-裁决全链、效应幂等与恢复、activation 生命周期、特权 IPC 与部署）已无开放实质项，仅余 AC-01 一条范围词 NIT 随 freeze 编辑落入；本方第三次、连续第三轮同意进入 P0 draft freeze。

（本轮无新增外部事实断言需核实；历轮 LangGraph 依据不变——Interrupts、1.0 稳定承诺、部署版本兼容性。）

上面是Claude Code的review意见
