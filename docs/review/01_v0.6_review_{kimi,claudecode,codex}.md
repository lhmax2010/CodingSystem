CodingSystem 01_Contract_Spec v0.6 第六轮评审报告（评审员：Claude Code）
1. 总裁决：NO-GO（无 BLOCKER；1 项 MAJOR——CF-FIX-00 注册集与旗舰 YAML 三处 status 不符，使 CF-DSL-FLAG 与 CF-FIX-00 互相矛盾，恰是本轮仲裁关注点 B-5 要求核验的断言本身不成立；另 1 MINOR。均为句级修复）
2. 任务 A：本方第五轮 issue 处置核验
第五轮编号	结论	说明
S-01 (MINOR)	接受关闭	CF-SER-07（分支 A：current/失效链/授权全不动）与 CF-SER-08（四类违例逐类拒绝，含本方所列"重复 submit"的异载荷形态）落表
S-02 (NIT)	接受关闭	迁移表 unknown 单元格补"∧ 非 replay_safe fallback，§4.3"，表与正文漂移消除
S-03 (NIT)	接受关闭	A1 双重定义清除，仅留 §2.7 模型并引注
3. 任务 B：五个仲裁关注点
B-1（series 防伪 + candidate 链）：接受，附 1 MINOR 边界。 以恶意/缺陷 worker 视角攻击：跨 series 记账被三层封死（身份由 socket 推导不信载荷、HandoffResult.series_id 对 Ledger 值等值校验、Report 头逐项一致）；静默丢 patch 的"在册 1 引用 None"形态被在册数对账正面拒绝；candidate 悬挂由状态机（task 终局未消费 → discarded）与 attempt 启动对账退役覆盖；每 task 至多一活跃 + 异载荷二次拒绝封住多 candidate。未能构造出新的伪造形态。残留一处边界（U-02）：request_id 幂等重放与 discarded 的交互——若 SDK 以确定性 request_id 跨 attempt 重放，将取回已退役的 candidate_id，进而在分支判定处撞上"活跃在册 0 而引用非 None"的拒绝，把一次合法重试变成不可重试的 sys.invalid_result；需一句定案幂等域。

B-2（plain 作用域下两份 YAML 复演不变）：接受。 旗舰与 A4 全部标量中仅 op: "<=" 为 quoted——新规则下 quoted 恒字符串，与原结果一致（str 字段）；value: 5.0/terminal: true/join:... 均为 plain，路径不变；两文件无 anchor/alias/merge/重复键。四类边界零自由度：quoted "true" 进 strict bool 字段必拒（确定性）、anchor 在 parse/event 阶段（引用信息未丢时）拒、重复键含 NFC 后碰撞在构造前拒。

B-3（provider digest 唯一算法）：接受。 provider_id 排序键 + 全局唯一约束消除列表序自由度；endpoint 规范化六规则确定；SecretStr 类型化投影为非启发式（类型驱动占位替换）；trust_policy/routing 经 canonical_json（digest 域禁浮点）稳定。同配置跨实现必得同 digest，CF-SM-12 双用例期望唯一。

B-4（reconciliation 补全后三点注入期望唯一）：接受。 ProbeDispatch 补 effect_class/record_view 使 worker 无需 Ledger 即可加载正确 EffectType；两命令通道修正了 v0.5"仅一条"与心跳的自相矛盾；总尝试数含首次的计数语义无歧义；三点注入期望逐点唯一——派发前 kill=未计数未派发、执行后 kill=已计数无回报（下轮 attempt+1、上限不破）、回写前 kill 后同组重放=幂等返回首结果、过期组拒。

B-5（CF-FIX-00 × CF-DSL-FLAG 接合）：反对——断言不成立（U-01）。 逐 token 对账：旗舰 routes 使用 build_failed_exhausted（compiler）、review_report_ready（review_static/review_final）、release_plan_ready（ci_plan），而 CF-FIX-00 注册集为 compiler{build_passed, build_failed} / review{pass, concerns, block} / ci{plan_ready}——三处不符。V-05（on ∈ status 全集）将拒绝旗舰，CF-DSL-FLAG（旗舰字节加载必过）与 CF-FIX-00（"两份 YAML 的 on 全落于集内"）互相矛盾；review 的集合还混淆了 ReviewReport.verdict 词表与 HandoffResult.status 词表。A4 侧无此问题（build_passed/bench_done/pass 均落于集内，success_on=[pass] 依赖 review 集含 "pass"——修正时须保留）。

4. 新 issue 清单
编号	等级	定位	问题描述	修改建议
U-01	MAJOR	§6 CF-FIX-00（连带 CF-FIX-01/04/05、CF-DSL-FLAG）	fixture 注册集与旗舰规范 YAML（HLD §4.2.1 冻结原文）三处 status 不符：compiler 缺 build_failed_exhausted（现为 build_failed）、review 缺 review_report_ready（现仅 verdict 词表 pass/concerns/block）、ci 为 plan_ready 而旗舰用 release_plan_ready——V-05 判定下旗舰必拒，CF-DSL-FLAG 与 CF-FIX-00 两断言矛盾，harness 按文实现即刻红灯	注册集改：compiler{build_passed, build_failed_exhausted} / review{review_report_ready, pass, concerns, block}（A4 summary 的 success_on=[pass] 依赖 pass 保留）/ ci{release_plan_ready}；CF-FIX-01/04/05 行的 fixture 描述与 CF-DSL-FLAG 注记同步核对
U-02	MINOR	§3.3 candidate 状态机 × §0.2	series_candidate_submit 的 request_id 幂等（同载荷重放返回同 candidate_id）与 attempt 启动对账退役的交互未定义：确定性 request_id 跨 attempt 重放取回已 discarded 的 candidate_id → 分支判定"活跃在册 0 而引用非 None"→ 合法重试被判为不可重试的 sys.invalid_result	定案二选一：幂等域限同一 attempt（attempt 序入幂等键，跨 attempt 同 request_id 视为新提交）；或重放命中 discarded 记录时重新登记并返回新 candidate_id
5. CLARIFY 清单
（空）

6. HLD 疑似缺陷登记
（空；E-02 候选已在册，不重复。）

7. 一句话总结
距 draft freeze 差一行注册集修正（U-01，使 CF-FIX-00 与两份规范 YAML 真正闭合）加一句 candidate 幂等域定案（U-02）；其余五轮工程全部闭合，修入后本方即转 GO，建议下轮可做定点复核。

上面是Claude Code的review意见

## 1. 总裁决：NO-GO

存在 4 项 BLOCKER、2 项 MAJOR。series 防伪和 loader 边界已经明显收敛，但控制面幂等契约、candidate 跨 attempt 恢复、schema 自足性及旗舰 fixture 仍未闭合。

## 2. 任务 A 结果表

| 本方第五轮编号 | 结论 | 理由与定位 |
|---|---|---|
| CS-24 | 不接受 | §2.4 已补 Finding/PendingEvent/Producer，但 §0.4 的“每个类型完整定义、无匿名 schema”仍未兑现：`SuiteResult`、`MetricEntry`、`RoundDigest`、`ReportEntry`、`EffectError` 仍是行内结构或字段类型不全；§5.1 的 `DecisionSubmitCommand/Result`、`ProbeDispatch/probe_report`、`AdjudicateRequest/Result` 也没有完整模型。见 R6-02。 |
| CS-25 | 不接受 | CF-EF-02 四组合和 CF-AUTH-07 已补；但 §6 CF-FIX-00 的注册状态集与 HLD 旗舰 YAML 不一致，导致 CF-DSL-FLAG 声称的“旗舰原文通过”不可成立。见 R6-04。 |
| R4-01 | 不接受 | 当前 attempt 内的 candidate 漏填已被在册数对账封死，但新增三态与 attempt 对账没有定义 discarded 后下一 attempt 的合法重新登记路径，正常恢复可自锁。见 R6-03。 |
| R4-03 | 不接受 | 已增加 provider_id 排序与唯一约束，但“升序”无字节序/字符集规则，endpoint 规范化仍非完整算法，SecretStr 投影又没有可作用的模型字段。见 R6-05。 |
| R4-04 | 不接受 | effect_class、record_view、heartbeat 和总尝试数已补齐；但已受理旧组重放与“旧 attempt 拒绝”存在重叠，最后一次派发事务后、spawn 前崩溃也没有终态规则。见 R6-06。 |
| R5-01 | 接受关闭 | §3.3 统一前置校验已使用 socket 上下文推导 task/dispatch，校验 HandoffResult.series_id、Report 头及 candidate parent；CF-SER-09 覆盖跨 series 伪造，忠实落实 HLD §4.2.4。 |
| R5-02 | 不接受 | adjudication_id 的生成方矛盾已经消除，但 `AdjudicateRequest/Result` 仍非完整字段类型模型，其 request_id 又没有适用于特权 IPC 的作用域和冲突语义；处置承诺只完成了一半。见 R6-01/R6-02。 |
| R5-03 | 接受关闭 | §1.1 已明确 resolution 只作用于无引号、无显式 tag 的 plain scalar；quoted/block 恒为字符串，anchor/alias/merge 在信息丢失前拒绝，重复键在原键和 NFC 后两层拒绝。 |
| R5-04 | 接受关闭 | 文档头、处置轮数和 §0.3 的陈旧展示号均已修正。 |

## 3. 任务 B 结果

1. **Series 防伪与 candidate 链：反对。**  
   统一前置校验可以阻断跨 series 结果、伪造 Report 头和 candidate 漏填；但恢复时间线仍不闭合：attempt 1 登记 C1 后崩溃，attempt 2 启动把 C1 置 discarded；若重放原 request_id，§0.2/§3.3 要求返回 C1，但 task_complete 又因“在册 0、引用非 None”拒绝；若提交新 request_id/新载荷，§3.3 与 §5.1 的“同 task 二次提交拒绝”又可能拒绝。candidate 状态转换是否与 attempt 启动、task 终局和 publishing pending 同事务也未写明。

2. **Loader resolution 表：接受。**  
   逐 token 复演结果唯一：`on:` 是普通字符串键；`join:pre_final_review` 的冒号后无空白，保持完整字符串；`5.0` 在节点层保留原词素并规范化为 `"5.00"`；`true` 仅在 plain 形态解析为 bool；`010` 不匹配整数规则而成为字符串；标量 `to` 最后列表化。quoted/block、anchor/alias/merge、自定义 tag、原键/NFC 重复键也都有封闭处置。此结论仅针对 loader；旗舰的后续 V-05 校验仍因 CF-FIX-00 失败。

3. **ProviderExecutionConfig digest：反对。**  
   provider_id 唯一性已有，但“升序”未规定 UTF-8 字节序或限制为 ASCII，跨语言排序可不同；endpoint 算法没有限定允许 scheme、默认端口表、根路径 `/` 的保留方式、IPv6/IDNA/百分号编码等边界；且 ProviderExecutionConfig 本身不存在 SecretStr 字段，类型化投影当前是空操作。CF-SM-12 也没有固定输入和期望 digest 的 golden vector。

4. **Reconciliation 协议：反对。**  
   两命令通道及 ProbeDispatch 输入已经足够启动 worker，但两处恢复结果不唯一：已接受的 attempt 1 `indeterminate` 报告在 attempt 2 派发后迟到重放时，同时满足“同组重放返回首结果”和“旧 attempt 拒绝”；最后一次 probe 已在派发事务计数、却在 spawn 前崩溃时，没有结果可供“indeterminate 耗尽”分支使用。CF-EF-10 只断言上限和幂等，未规定三个点各自的记录状态、调度和终态。

5. **CF-FIX-00 与 CF-DSL-FLAG：反对。**  
   HLD §4.2.1 旗舰使用 `build_failed_exhausted`、`review_report_ready`、`release_plan_ready`；CF-FIX-00 却分别登记 `build_failed`、review 的 `{pass, concerns, block}`、`plan_ready`。因此 compiler、review_static/review_final、ci_plan 会在 V-05 被拒。A4 自身可通过；metric 与 gateable 字段也可判定，但“两份规范 YAML 全部通过”的断言为假。

## 4. 新 issue 清单

| 编号 | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|---|---|---|---|
| R6-01 | BLOCKER | §0.2 `request_id`、§5.1；HLD §8.5 | HLD §8.5 明确规定 control-plane“命令幂等（request_id）”。01 §0.2 却只把 request_id 定义为 Skill 调用方生成、作用域为 `(bearer principal, endpoint)`；§5.1 的 approval、broker 及绝大多数 worker 命令载荷均不含 request_id，也没有公共 RPC envelope。不同实现只能自行选择自然键、连接级键或 request_id，构成对冻结 HLD 的遗漏。 | 定义统一、具名的 control-plane 请求 envelope；规定各 socket 角色下 request_id 的生成方、作用域、保留期、payload digest 冲突、事务后 ACK 丢失重放响应。per-task worker 作用域须包含 task/attempt/command。补 approval、broker、worker 各一组同载荷/异载荷重放 case。 |
| R6-02 | BLOCKER | §0.4、§2.4、§4.2–§4.3、§5.1、附录 A0 | “全部类型完整定义、无匿名 schema”的自足声明仍不成立。`SuiteResult.name/board_id` 类型未给，`EffectError.detail` 只是裸 `dict`；多个 Report 辅助类型仍为行内结构；A0 列出的 DecisionSubmit、ProbeDispatch/probe_report 等只有散落的字段集合，没有完整模型、约束或封闭响应类型，Adjudicate 类型甚至未进入 A0。Codex 无法据此唯一生成 contracts 包。 | 把 A0 所列每一类型落实为字段类型完整的具名模型/判别联合；补齐约束、Literal 枚举、可空规则和响应错误联合，并以自动检查保证 A0 类型均能解析到唯一正文定义。 |
| R6-03 | BLOCKER | §0.2 `candidate_id`、§3.3、§5.1 `series_candidate_submit`、CF-SER-08；HLD §4.3.2 | HLD §4.3.2 的重试是“同 task 新 attempt”。现有 candidate 状态机在上一 attempt 的 registered 记录被 discarded 后，没有定义下一 attempt 如何登记新 candidate；旧 request_id 返回已 discarded ID，新请求又可能被“每 task 至多一个/二次提交拒绝”拦截。正常崩溃恢复因此可能永久转 sys.invalid_result。 | 定义 CandidateRecord 的 task/dispatch/attempt/request_id/payload_digest/parent/state 字段；明确“每 task 至多一个活跃项”实际按当前 attempt 生效；attempt 启动事务原子 discard 旧项并允许新项；规定旧 attempt 重放、当前 attempt 同载荷重放及异载荷冲突。补“登记后崩溃→新 attempt 重登→完成”和各提交点 kill case。 |
| R6-04 | BLOCKER | §6 CF-FIX-00、CF-DSL-FLAG、V-05/V-14；HLD §4.2.1 | CF-FIX-00 与冻结旗舰原文状态名不匹配：`build_failed`≠`build_failed_exhausted`，缺 `review_report_ready`，`plan_ready`≠`release_plan_ready`。按 V-05，旗舰 YAML 必被拒，与 HLD §4.2.1“示例必须通过校验”直接冲突。 | 将 fixture status_set 与两份规范 YAML 取值逐项对齐：compiler 至少含 `build_passed/build_failed_exhausted`，review 同时覆盖 `review_report_ready` 与 terminal 使用的 `pass`，CI 使用 `release_plan_ready`；由 harness 从两份原文字节抽取 on/success_on 并逐项断言注册集包含。 |
| R6-05 | MAJOR | §5.3 ProviderExecutionConfig、CF-SM-12 | provider digest 尚非唯一算法：provider_id 排序缺字符域/排序字节规则；endpoint 规范化的输入语法和边界未封闭；SecretStr 投影没有对应字段。等价端点如 `https://EXAMPLE.com:443/` 可被实现为保留或删除根 `/`，非 ASCII provider_id 在不同语言也可能产生不同顺序。 | 限制 provider_id 为明确 ASCII grammar 并规定 UTF-8 字节序；完整规定 endpoint 接受域、默认端口表和路径/主机规范化或直接要求输入已是唯一 canonical URI；明确 ProviderExecutionConfig 是无 secret 投影，或加入具名 SecretStr 字段及投影步骤。为 CF-SM-12 给出固定输入与期望 digest。 |
| R6-06 | MAJOR | §4.3 reconciliation、CF-EF-10 | probe report 的幂等优先级和派发崩溃终态不封闭。已受理旧 attempt 的重放在后续 attempt 派发后既是“同组重放”又是“过期组”；最后一次在 spawn 前崩溃则没有 ProbeResult，却已耗尽计数。实现可选择返回缓存、拒绝、重派或直接 unknown，均不违反当前部分文字。 | 定义持久化 report receipt 及判定顺序：先查已受理同组并校验 payload，命中返回首响应；仅无 receipt 的 stale 组拒绝。明确派发事务后未 spawn/未回报在达到上限时的合成结果和终态。CF-EF-10 对三个 kill 点分别断言 attempts、next_probe_at、receipt、spawn 次数及最终状态。 |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

空。上述 HLD 相关项均为 01 未完整承载冻结语义，不是 HLD 自身缺陷；E-02 不重复登记。

## 7. 一句话总结

距离 draft freeze 还需关闭 control-plane 幂等/schema、candidate 跨 attempt 恢复和旗舰 fixture 三项主路径 BLOCKER，并定死 Provider digest 与 probe 崩溃重放语义。

上面是Codex的review意见

v0.6 全文已读（以 diff 精确定位本轮改动面），第五轮处置表已核对。任务 B-5 的接合核验发现一处注册表内部矛盾。以下为第六轮评审。

---

# 复审报告：01_Contract_Spec v0.6（Kimi 方）

## 1. 总裁决：**NO-GO**

本方第五轮 3 条全部妥善关闭；B-1～B-4 复演接受。但 B-5 的接合核验发现 CF-FIX-00 注册集与 CF-DSL-FLAG 直接矛盾——旗舰原文在定案注册集下过不了 V-05，两个用例不可能同时成立。§6 是 07 的判定权威，注册表自相矛盾必须 NO-GO。

## 2. 任务 A：本方第五轮 issue 处置核验

| 编号 | 结论 | 说明 |
|------|------|------|
| M-01 | 接受关闭 | CF-EF-02 轴改 supports_probe × replay_safe 四组合，单元格规则同步 fallback 支（耗尽∧replay_safe→abandoned+版本化重放）；§4.2 迁移表 unknown 单元格注记同步——§4.3 与注册表矛盾消除 |
| M-02 | 接受关闭 | §0.2 补 candidate_id 行（ulid／编排 series_candidate_submit 事务／request_id 幂等／每 task 至多一活跃），三要素齐备 |
| M-03 | 接受关闭 | §2.1 末段 pending_register 签名已统一为 `{run_id, event: PendingEvent}`，与 §5.1 一致 |

E-02（HLD"依类别策略放行"两读）为已登记候选，本轮按任务要求不表态。

## 3. 任务 B：仲裁关注点

**B-1（series 防伪 + candidate 链）：接受。** 以恶意/缺陷 worker 视角逐路攻击：伪造 HandoffResult.series_id → 被"= TaskInput.series_id（Ledger 记录值）"拒绝；Report 头跨 series 涂写 → 逐项一致性校验拒绝（CF-SER-09）；candidate 引他 task / 多引用 / 在册 0 引非 None → 拒绝；**在册 1 引 None（漏填）拒绝**——静默丢 patch 的最后一个形态封死；candidate 状态机三态（registered/consumed/discarded）+ attempt 启动对账退役遗留 + request_id 幂等（同载同 id、异载拒绝）闭合；悬挂 candidate 经"task 终局未消费即 discarded"无泄漏。跨 series 记账无可构造形态。闭合。

**B-2（loader plain 作用域定案）：接受。** 两份规范 YAML 复演结果与上轮不变（两文件全部为 plain scalar，quoted 仅 `"<="` 恒字符串）；四类边界零自由度：quoted/block 恒字符串（`terminal: "true"` 得字符串、strict bool 拒绝——行为确定）、anchor/merge/自定义 tag 在 parse/event 阶段拒（引用信息未丢失时报错定位准确）、重复键（原键 + NFC 后双检）拒、resolution 表仅作用 plain scalar 且为全函数。无残余歧义。

**B-3（provider digest 唯一算法）：接受。** 排序键（provider_id 升序 + 全局唯一拒绝重复）+ endpoint 规范化（小写 scheme/host、省缺省端口、去尾斜杠、禁 query/fragment/userinfo）+ SecretStr 类型化投影（非启发式、固定占位）三件套使"同配置跨实现同 digest"可实证；CF-SM-12 双用例（端点改向失配 / 密钥轮换不失配）期望唯一，忠实 HLD D12。

**B-4（reconciliation 补全）：接受。** ProbeDispatch 全字段（effect_class 供 worker 加载注册类型、record_view 只读投影使 worker 无需 Ledger 读权限、params_ref 取参数）与 EffectType.probe(params, record) 签名闭合；通道两命令（heartbeat + probe_report）不突破数据面封闭枚举；probe_retry_max=总尝试数含首次的语义歧义消除；同组重放幂等返回首结果、过期组拒绝——CF-EF-10 三点（派发前/执行后/回写前）期望点唯一（前=未计数可重派；中/后=已计数保守耗尽、上限不破、重放幂等）。

**B-5（CF-FIX-00 × CF-DSL-FLAG 接合）：反对——发现矛盾（即新 I-01，BLOCKER）。** 逐名核对：旗舰原文（HLD §4.2.1）使用的业务 on 值为 compiler:`build_passed`/`build_failed_exhausted`、ut:`ut_passed`/`ut_fix_patch_ready`、benchmark:`bench_done`、review:`review_report_ready`、ci:`release_plan_ready`；CF-FIX-00 注册集为 compiler{build_passed,**build_failed**}、review{pass,concerns,block}、ci{**plan_ready**}。三个 on 值（build_failed_exhausted、review_report_ready、release_plan_ready）不在注册集内 → V-05 拒绝加载旗舰 → CF-DSL-FLAG"全部通过"的期望不可达；两 case 必有一假。A4 子集无此问题（其 on/success_on 全落于注册集）。

## 4. 新 issue 清单

| # | 等级 | 定位 | 问题描述 | 具体修改建议 |
|---|------|------|---------|-------------|
| I-01 | BLOCKER | §6.5 CF-FIX-00 vs §6.1 CF-DSL-FLAG | 注册集自相矛盾：CF-FIX-00 的 compiler{build_passed, build_failed}、review{pass, concerns, block}、ci{plan_ready} 不覆盖旗舰原文使用的 `build_failed_exhausted`、`review_report_ready`、`release_plan_ready` 三个 status——CF-DSL-FLAG 断言旗舰原文加载通过，但在 CFIX-00 注册集下 V-05 必拒（route.on ∉ status 全集）。§6 作为 07 判定权威，两 case 互斥使 harness 无可执行语义 | 修正 CF-FIX-00 注册集使两案闭合：compiler 加 build_failed_exhausted、review 加 review_report_ready、ci 加 release_plan_ready（旗舰原文属 HLD 冻结文本不可改，只能动注册集）；修正后 A4 复核保持通过 |

## 5. CLARIFY 清单

空。

## 6. HLD 疑似缺陷登记

无新增（E-02 候选已登记，不重复）。

## 7. 一句话总结

距 freeze 差一处注册集修正：CF-FIX-00 补三个 status 名（I-01，改注册集而非旗舰——后者是 HLD 冻结文本），修正即 GO；本轮其余全部内容（series 防伪、candidate 链、loader 作用域、provider digest、reconciliation 协议）经复演均闭合。

上面是Kimi Code的review意见
